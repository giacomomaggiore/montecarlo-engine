# Asset Allocation Monte Carlo Simulator

## Project Vision
* Build a Monte Carlo simulator specifically for retail investors.
* Run all calculations directly in the browser.
* Keep infrastructure requirements minimal by using static hosting within provider quotas.
* Keep portfolio settings and simulation results in the browser; static asset requests still reach the hosting provider.
* Provide a responsive interface within measured device and simulation limits.

## Main Goals
* Use modern web technologies such as Web Workers and typed arrays to run thousands of simulations without freezing the interface.
* Implement accurate mathematical models for asset correlation and risk metrics.
* Keep the last completed result visible while inputs change; start expensive computation only when the user selects **Run**.
* Use a curated dataset to keep download sizes small and optimize performance.

## Data Architecture
* **Selected Dataset:**
  * Avoid downloading the entire stock market.
  * Ship manifest-described, little-endian `Float32` binary files containing 200 to 300 popular tickers and ETFs.
  * Rely on HTTP compression from the static host. Do not parse Parquet or large JSON return matrices in the browser.
* **Backfilling via Asset-Class Proxy:**
  * Handle new ETFs that lack long historical data.
  * Map new funds to their parent index to fill in missing past data.
  * Example: Use MSCI World data to simulate the history of a recent global ETF during the 2008 and 2000 crashes.
  * Record the proxy, splice date, and backfill method in `assets.json`. The educational page discloses that proxy backfills are used.
* **Data Frequency:**
  * Use weekly data by default and offer monthly data when leverage is disabled.
  * Enabling leverage selects and locks weekly frequency. Daily simulation is outside the first-release scope.
* **Currency:**
  * Let users choose USD or EUR as the portfolio base currency.
  * Prepare separate return matrices for each base currency. Returns must include currency conversion before they reach the browser.
* **FX Conversion and Hedge Treatment:**
  * The offline pipeline converts every asset return into the selected base currency before writing a return matrix. For an unhedged return series in currency $C$ and base currency $B$, with $X_t$ quoted as units of $B$ per unit of $C$, calculate $1+r_t^B=(1+r_t^C)(X_t/X_{t-1})$. When $C=B$, use the return unchanged.
  * Metadata distinguishes `listingCurrency`, `exposureCurrency`, and `returnCurrency`. For an unhedged fund, `returnCurrency` must equal its economic `exposureCurrency`; the listing venue does not decide the FX conversion. For a currency-hedged share class, `returnCurrency` is the hedge currency and `fxTreatment` is `already-hedged`; do not apply an additional underlying-exposure FX return, but still convert the hedged return from its return currency to the selected base currency when they differ.
  * Record the direct, inverted, or triangulated FX source, quote orientation, provider, source symbol, retrieval date, and transformation in provenance. Do not forward-fill unavailable FX observations; mark the converted return unavailable and let common-row alignment remove it. Fail the pipeline when a required FX route is missing or ambiguous.
* **Inflation and Risk-Free Rates:**
  * Include historical Consumer Price Index (CPI) and risk-free rate data.
  * Allow users to switch between nominal returns and real, inflation-adjusted returns.
  * Use base-currency series: CPI and EFFR for USD; HICP and a prepared EUR short-rate series for EUR.
  * Convert monthly CPI/HICP observations into weekly log-inflation increments with a documented deterministic interpolation rule. This changes frequency but does not create new economic information.
  * Convert annualized short-rate observations into effective weekly or monthly rates before borrowing and Sharpe calculations.
  * Release a currency only when its versioned inflation, short-rate, FX-converted return, and metadata artifacts are complete.

## Data Pipeline
* Build the artifacts described in "Dataset Format and Loading" with a `pipeline/` directory of Python scripts (pandas/numpy), run offline before deployment. This code never ships to the browser and follows the Python coding standards, not the TypeScript ones.
* Record each asset's **exposure currency** and whether it is a currency-hedged share class in `assets.json`, and convert returns to each base currency using that exposure currency rather than the ticker's listing venue. A Milan-listed ETF that tracks a USD-denominated index (for example `CSSPX.MI`) has USD exposure; converting it as if it were EUR-denominated would misstate its currency risk.
* Fail the pipeline (non-zero exit) when an asset has no resolvable exposure currency or proxy mapping, consistent with the "never silently clip invalid values" rule in Financial Rules.
* **Asset Metadata Catalogue:**
  * Maintain `pipeline/metadata/assets.csv` as the editable source-of-truth table, with one row per canonical `assetId`, and `pipeline/metadata/fx_routes.csv` as the referenced FX-route table. The pipeline joins each source return DataFrame to `assets.csv` by stable asset ID or explicitly mapped source symbol, then compiles the validated records into the browser-facing JSON catalogue. A separate route table keeps direct, inverted, and triangulated FX details out of duplicated ETF rows.
  * Build and ship a versioned `assets.json` catalogue with one canonical record per simulatable ETF or asset. Use a stable `assetId` as the binary-matrix column key; keep the displayed ticker and provider/source symbol as separate fields because tickers and listings can change or collide.
  * Required identity and presentation fields are `assetId`, ticker, complete name, asset class, source symbol, provider, listing exchange, listing currency, exposure currency, return currency, `fxTreatment`, `isCurrencyHedged`, and data/provenance references. Optional descriptive fields include ISIN, fund domicile, investment region/category, distribution policy, replication method, inception date, and AUM.
  * Record `terAnnual` as a non-negative decimal fraction together with `terAsOf` and `terSource`. It is informational in the first release: adjusted-close total returns already reflect fund expenses where the source provides them, so the simulator must not subtract TER a second time. A later forward-looking return-assumption feature may use an explicitly documented TER policy.
  * A proxy/backfill record is either absent or contains the proxy `assetId`, splice date, method, and rationale. An FX-provenance record contains the route and transformation used for each produced base currency. Validate unique IDs, supported currencies, no duplicate matrix columns, valid provenance references, non-negative TER, and hedge consistency: unhedged records use `returnCurrency=exposureCurrency`; hedged records require a hedge currency and use `fxTreatment='already-hedged'`.
  * Keep user-facing metadata separate from periodic return data. `assets.json` is loaded first for ETF search, labels, eligibility, and explanations; the large return matrices remain compact binary artifacts loaded only after the user chooses frequency and base currency.

## Simulation Engines
* **Historical Joint Bootstrap:**
  * Build one aligned matrix for the selected assets, inflation, and rates. Remove any row containing a missing required value after proxy backfilling.
  * Sample complete rows with replacement. Never sample each asset independently.
  * Preserve contemporaneous cross-asset dependence and empirical one-period outcomes. Independent row sampling does not preserve serial dependence, volatility clustering, or crisis duration.
* **Parametric Student's $t$ Simulation:**
  * Fit the model to aligned periodic log total returns.
  * Let users choose historical annualized geometric returns or enter annual geometric returns for each asset.
  * Estimate one shared degrees-of-freedom value from pooled standardized historical residuals using excess kurtosis: $\nu = 4 + 6 / \gamma_2$. Clamp $\nu$ to $[5, 100]$ and use $\nu = 100$ when excess kurtosis is not positive.
  * Convert covariance $\Sigma$ to the Student's $t$ scale matrix $S = \Sigma(\nu - 2) / \nu$.
  * Apply Higham's nearest-correlation algorithm while preserving asset variances, then use Cholesky decomposition with a documented numerical tolerance.
  * Convert generated log returns back to simple returns before portfolio accounting.
* **Future Markov-Chain Simulation:** Show a disabled, clearly labelled future option in the interface. Do not ship Markov code in the first release; add it only after both live engines and portfolio accounting are validated.

## Portfolio Logic and Cash Flows
* **Cash-flow modes:**
  * **Lump sum:** invest an initial amount with no later external cash flows.
  * **DCA:** add a fixed contribution at the end of each period. It does not earn that period's return.
  * **Contribution-only value averaging:** define the target path $A_t=A_0+t\Delta$, where $\Delta$ is the user-entered periodic target-value increase. After returns and borrowing interest, contribute $C_t=\max(0,A_t-E_t^-)$, where $E_t^-$ is pre-contribution equity. Never withdraw when equity exceeds the target.
  * Invest each contribution at target asset proportions. Purchase costs apply and increase cost basis.
* **Benchmark:** Allow one optional ETF. It receives the portfolio's actual external cash flows on the same dates, including value-averaging contributions calculated from the portfolio. It is a separate 1x buy-and-hold comparison with no leverage, borrowing, rebalancing, taxes, or transaction costs.
* **Taxes:**
  * Use one user-entered capital-gains tax rate and average cost basis for each asset.
  * Initial holdings use the initial invested amount as basis unless the user explicitly enters another basis. Every simulated purchase updates average basis.
  * Apply tax to sales caused by rebalancing, margin calls, and final liquidation. Dividends are reinvested through total returns and are not taxed separately.
  * Carry realized losses forward within each path to offset later realized gains. Do not generate tax refunds.
  * Deduct tax immediately after a taxable sale and report after-tax terminal wealth following final liquidation.
* **Rebalancing Methods:**
  * **Time-based:** Rebalance after a user-entered number of simulation periods.
  * **Tolerance Bands:** Rebalance when an asset differs from its target by a user-entered number of percentage points. Evaluate bands once per period after contributions and forced margin actions.
* **Leverage Options:**
  * Support long-only target gross exposure $L$ with editable maintenance margin $m$, where $0<m\le1$ and $1\le L\le\min(4,1/m)$. Changing $m$ automatically limits the leverage input.
  * Warn when $L=1/m$: the portfolio starts exactly at maintenance margin and has no buffer.
  * Define debt as gross asset value minus equity and maintenance margin as equity divided by gross asset value.
  * Charge the base-currency short rate plus a user-entered spread on debt.
  * Support time-based and tolerance-band leverage-resetting strategies.
  * Check margin every weekly simulation period. If maintenance margin falls below $m$, sell assets proportionally and repay debt until target leverage is restored after tax and costs.
  * Do not model cash deposits during a margin call. If equity is non-positive or target leverage cannot be restored, mark the path insolvent with zero terminal wealth.
* **Transaction Costs:** Apply both a fixed base-currency cost per nonzero asset order and a percentage of absolute trade value. Buy costs increase cost basis; sell costs reduce proceeds.

For every simulation period, apply operations in this order:
1. Apply asset returns and borrowing interest.
2. Determine and add the scheduled DCA or value-averaging contribution, then invest it at target proportions.
3. Check maintenance margin and execute forced deleveraging when required. Scheduled contributions may prevent a call; unscheduled rescue deposits are not allowed.
4. Evaluate and execute portfolio rebalancing or leverage resetting.
5. Update debt, average cost basis, transaction costs, loss carryforward, and realized-gain tax for every trade.
6. Record end-of-period holdings, debt, equity, neutral return, and metrics.

## Visualization and Metrics
* **Optimized Charts:**
  * Avoid heavy "spaghetti plots" that slow down the browser.
  * Display seven representative paths — one real, actually-simulated trajectory per terminal-wealth quantile level (p1, p10, p25, p50, p75, p90, p99) — rather than a synthetic cross-sectional band. Each line is the path whose own terminal wealth landed nearest that quantile of the terminal-wealth distribution; see the Core Engine Reference's `selectRepresentativePaths` entry for the selection rule and why a per-period cross-sectional aggregate was replaced with real paths.
  * Limit the display to a maximum of 50 individual line paths.
* **Interactive Highlights:**
  * Allow users to click retained individual paths and inspect their returns, drawdowns, trades, taxes, and historical row selections.
  * The seven representative paths are real, individually meaningful trajectories, not an aggregate statistic — but because each is chosen only by its own terminal value, two representative paths can cross at intermediate periods (e.g. the p10 path can sit above the p90 path partway through the horizon); label them as individual paths, not as a guarantee that p10 ≤ p25 ≤ ... ≤ p90 holds at every period, only at the terminal one. With few simulated paths, adjacent levels (e.g. p1/p10) can resolve to the same nearest path.
* **Key Metrics Panel:**
  * Show compound annual growth rate (CAGR) only for lump-sum paths without later contributions. Show money-weighted return (IRR) when recurring contributions are enabled.
  * Compute volatility from contribution-neutral periodic portfolio returns using sample standard deviation and annualize by $\sqrt{12}$ or $\sqrt{52}$.
  * Compute the Sharpe ratio from arithmetic excess returns using the aligned period risk-free series.
  * Compute maximum drawdown from a unitized portfolio index so that contributions do not appear as investment gains.
  * Calculate percentiles with linear interpolation and use the same method throughout the application.
  * Report after-liquidation terminal wealth at p10, p25, p50, p75, and p90.
  * Define loss probability as $P(W_T<\sum_t C_t)$ and ruin probability as the fraction of insolvent paths. A target shortfall is not ruin.
  * Report annualized IRR for DCA and value averaging; display unavailable when no finite admissible root exists.
  * When a benchmark is selected, report terminal-value difference and probability that the portfolio outperforms it under identical external cash flows.
* **Data Export:**
  * Export settings, seed, dataset version, summary metrics, and terminal outcomes for all completed paths as CSV.
  * Export full period-by-period data only for the retained sample of up to 50 paths to keep browser memory bounded.

## Technical Design

### Technology Choices
* Build the application with React, TypeScript, and Vite.
* Deploy the static site on Vercel.
* Use uPlot for fast simulation charts.
* Keep all simulation code framework-independent so that it can be tested without rendering the UI.
* Run simulations in a single Web Worker. The main thread is reserved for input, results, and chart rendering.

### Maintenance Policy
* Enable TypeScript strict mode. Validate untrusted JSON and binary metadata at runtime instead of relying on compile-time types.
* Use React local state or `useReducer` for the simulator state machine. Do not add a global state library, dependency-injection layer, or generic repository/service layer without a demonstrated need.
* Keep financial and statistical functions pure and colocate focused tests with their modules. Use Vitest for unit/statistical tests, Testing Library for user interactions, and a small Playwright smoke suite for routing, running, cancellation, and one result render.
* Pin the runtime major version and package-manager version; commit the lockfile. Keep dependencies few, review updates, and avoid packages for short deterministic algorithms that are easier to test locally.
* CI must run formatting, linting, TypeScript checking, unit tests, and a production build. Release only immutable, versioned data artifacts that pass the same validation used by the browser.
* Treat README financial conventions as authoritative. Any change to timing, formulas, data schema, PRNG behavior, or metric definitions requires a version note and matching tests.

### Recommended Project Structure

```text
src/
  app/
    App.tsx
    routes.tsx
  features/
    simulator/
      SimulatorPage.tsx
      SimulatorInputs.tsx
      SimulatorResults.tsx
      simulatorState.ts
    education/
      EducationPage.tsx
    resources/
      ResourcesPage.tsx
  core/
    data/
      loadDataset.ts
      datasetTypes.ts
    math/
      matrix.ts
      quantiles.ts
      statistics.ts
    portfolio/
      cashFlows.ts
      rebalancing.ts
      taxes.ts
      leverage.ts
      transactionCosts.ts
    simulation/
      simulationEngine.ts
      historicalBootstrap.ts
      parametricStudentT.ts
      simulationTypes.ts
      runSimulation.ts
  workers/
    simulation.worker.ts
  charts/
    portfolioChart.ts
    leverageChart.ts
  styles/
    global.css
  main.tsx

public/
  data/
    manifest.json
    assets.json
    returns-monthly-usd.f32
    returns-monthly-eur.f32
    returns-weekly-usd.f32
    returns-weekly-eur.f32
```

This structure deliberately avoids separate service, repository, and utility layers. `core/` contains small, pure financial and mathematical functions; `features/` contains the React interface; the worker only coordinates data and calls the simulation engine.

`simulationEngine.ts` defines the only extension boundary required for simulation methods. Every engine produces one `PeriodScenario` containing joint asset returns, inflation, the period risk-free rate, and optional metadata such as a historical row index or Markov regime ID. `runSimulation.ts` passes this scenario to the unchanged portfolio-accounting loop. A new engine must not implement contributions, rebalancing, taxes, leverage, charts, or exports.

### Interface
* Provide three routes: `/` for Engine, `/education` for explanations, and `/resources` for curated external references.
* Use a desktop-first two-column input workspace followed by full-width charts and metrics. Stack these sections on narrow screens.
* Keep completed results visible while settings are edited, but label them as belonging to the previous completed run.

### Dataset Format and Loading
* Convert source CSV files into aligned monthly and weekly return series before deployment.
* Derive adjusted-close total returns using the last valid observation in each calendar week or month. Do not forward-fill prices across periods.
* Ship `manifest.json` and `assets.json` first. The manifest defines the schema version, checksum, frequency, base currency, ordered date vector, ordered columns, dimensions, byte offsets, units, and special CPI and rate columns.
* Build every artifact reproducibly. Record source/provider, license, retrieval date, timezone, trading calendar, total-return interpretation, FX series, proxy and splice rules, outlier checks, and content hash.
* Validate the manifest at runtime with an explicit schema. Verify checksum, exact dimensions and byte length, monotonic dates, expected units, and finite selected rows before simulation. Dataset updates are atomic and versioned.
* Store every matrix in little-endian, column-major `Float32` format, with one contiguous series per asset or special column. Represent unavailable observations as `NaN` on disk.
* Convert loaded values to JavaScript `number` values for compounding, debt, tax, cost-basis, and matrix calculations. `Float32` is a transport format, not the accounting precision.
* At runtime, retain only rows that are finite for every selected asset and required special series. Use this same common period for bootstrap sampling and parametric estimation. Display its start date, end date, and observation count.
* Require at least 60 monthly or 260 weekly common observations. Disable simulation when the selected portfolio has less history.
* Fetch only the file for the selected frequency and base currency, then cache it in memory for the current session.
* Limit the investment horizon to 30 years and the portfolio to six assets.

### Simulation Limits
* Default to 2,000 simulation paths and a 10-year horizon.
* Set an absolute maximum of 50,000 paths and enforce $\text{paths} \times \text{periods} \le 10{,}000{,}000$. Reduce the selectable path maximum when the requested frequency and horizon exceed this work budget.
* Process paths in bounded batches. Retain terminal metrics for every path, period values for aggregate quantiles, and full details for at most 50 sampled paths.
* Transfer only percentile arrays, terminal metrics, and at most 50 retained paths from the worker. Do not transfer complete holdings, debt, or basis histories for every path.
* Publish progress after each batch. On cancellation, terminate and replace the worker; ignore stale messages by comparing run IDs.
* Support monthly and weekly frequencies from the first release. Enabling leverage automatically selects and locks weekly frequency.

### Financial Rules
* Simulate and perform all accounting in nominal base-currency values.
* For real-value display, deflate nominal path values by a jointly sampled historical inflation path. In parametric mode, use a user-entered constant annual inflation assumption.
* In parametric mode, use a user-entered constant annual base-currency risk-free rate for debt costs and Sharpe-ratio calculations.
* Keep recurring contributions nominal; do not increase them with inflation.
* Show a weekly leverage-ratio chart whenever leverage is enabled.
* Use a locally implemented `xoshiro128**` pseudo-random number generator initialized from a deterministic 32-bit seed. Do not use `Math.random()` in either simulation engine.
* Expand the 32-bit seed deterministically into all four generator state words and reject the all-zero state. Version this expansion rule.
* Include the seed, PRNG/model version, dataset checksum, full configuration, and quantile algorithm version in every result and export.
* Treat non-finite returns or accounting values, invalid covariance, infeasible trades, non-positive equity, and failed numerical routines as explicit path or run failures. Never silently clip invalid values into plausible results.

### Future Markov-Chain Extension
Keep Markov outside the first-release source tree. Its eventual implementation must use the same aligned data and `PeriodScenario` interface, emit complete regime-compatible rows, and pass deterministic transition and accounting tests. The educational page may describe it before release, but the Engine control remains disabled.

### Implementation Order
1. Create the Vite application shell, three routes, responsive two-column simulator inputs, and static placeholder results.
2. Define and validate one complete base-currency binary dataset contract before adding a second currency.
3. Implement and test aligned historical joint bootstrap, portfolio weights, contributions, and quantile metrics.
4. Move the simulation runner into a Web Worker and connect it to uPlot.
5. Add parametric Student's $t$ simulation, including degrees-of-freedom estimation and covariance repair.
6. Add benchmark comparison, rebalancing, transaction costs, taxes, leverage, margin calls, and CSV export in that order.
7. Add the second base currency only after its complete FX, inflation, rate, and provenance pipeline passes validation.
8. Add the Markov-chain engine only after the two existing engines and their accounting paths are fully tested.

### Validation Requirements
* Keep `core/` functions deterministic and independently testable.
* Add hand-calculated tests for one-asset compounding, end-of-period contributions, rebalancing, average cost basis, loss carryforward, final liquidation tax, transaction costs, borrowing interest, margin calls, and insolvency.
* Add statistical tests for aligned bootstrap rows, seeded reproducibility, Student's $t$ scale conversion, covariance repair, and generated dependence.
* Add worker tests for progress, stale run IDs, cancellation, and transferred buffers.
* Add invariant tests for benchmark cash-flow identity, value conservation without costs, non-negative long-only holdings, debt repayment, basis conservation, and exact maintenance-margin boundaries.
* Validate one small simulation against an equivalent spreadsheet or Python calculation before optimizing it.

## Frontend Layout and Interaction Specification

This section is authoritative when an older statement elsewhere conflicts with a frontend default.

### Navigation and Responsive Layout

* The persistent header contains the product name and links to **Engine**, **Educational**, and **External Resources**. Show the active route and provide keyboard-operable mobile navigation.
* On desktop, the Engine route begins with a two-column input workspace. The left column contains portfolio construction; the right contains simulation and portfolio settings. The chart and metrics table span the full width below both columns.
* On narrow screens, stack portfolio construction, simulation settings, portfolio settings, chart, and metrics in that order.

### Portfolio Construction

* Provide a keyboard-searchable ETF picker backed by `assets.json` and allow at most six holdings.
* Each row shows ticker, name, allocation, and remove control. Display the allocation total continuously and require it to equal 100% within a 0.01 percentage-point tolerance.
* Provide a separate optional one-ETF benchmark selector. Do not include it in portfolio holding limits, weight validation, or accounting controls.

### Simulation and Portfolio Inputs

* Use a three-button engine selector: **Bootstrap** and **Parametric** are enabled; **Markov Chain** is disabled and labelled “future”. Render only the selected live engine's controls.
* Bootstrap inputs: deterministic seed and a read-only common-history summary.
* Parametric inputs: deterministic seed, historical or user-entered annual geometric return for each holding, automatic or manual degrees of freedom, annual inflation, and annual risk-free rate. Derive covariance from aligned data rather than exposing an editable matrix.
* Common defaults are weekly frequency, 2,000 paths, and 10 years. Paths and horizon are adjustable within the global work budget. Monthly frequency appears as an advanced unleveraged option.
* Portfolio settings include initial investment; lump sum, DCA, or contribution-only value averaging; rebalancing mode (`none`, time, or tolerance band); nominal or real display; tax rate; fixed and proportional trade costs; and initial cost-basis override.
* Leverage settings include target leverage, maintenance margin, borrowing spread, and reset mode (`none`, time, or tolerance band). Show only controls relevant to the active mode and automatically enforce $L\le\min(4,1/m)$.

### Execution State

* Use explicit `idle`, `loading-data`, `running`, `completed`, `cancelled`, and `failed` states with one current run ID.
* Provide **Run** and **Cancel** actions and batch progress. Input changes during a run terminate and replace the worker; messages with stale run IDs are ignored.
* Disable Run while inputs or data are invalid. Do not automatically rerun expensive simulations when a field changes.

### Results

* Draw p10, p25, p50, p75, and p90 quantile series, up to 50 retained individual paths, and the optional benchmark line. Percentile series are aggregates and cannot be selected as if they were paths.
* The initial metrics table includes terminal-wealth percentiles, loss probability, ruin probability, CAGR or IRR, annualized volatility, maximum drawdown, Sharpe ratio, benchmark terminal difference, and probability of benchmark outperformance.
* Show `N/A` with a short reason when a metric is undefined. Keep metric definitions linked to the Educational route.

### Validation and Accessibility

* Validate finite numeric values, bounds, six-holding maximum, allocation total, data-history minimum, path-period budget, tax and cost ranges, rebalancing periods and bands, and leverage-margin compatibility.
* Place errors beside their controls and summarize them in an `aria-live` region. Use semantic `nav`, `main`, headings, `fieldset`, and `legend`; visible labels and focus indicators; keyboard-operable controls; and status cues that do not rely only on color.
* Provide a tabular alternative for essential chart values and respect reduced-motion preferences.

## Core Engine Reference — Phase 1 (As Built)

This section documents every framework-independent function implemented so far in `src/core/`. It is a reading guide, not the source of truth — each module's colocated `*.test.ts` file is the actual proof of behavior. For each function: **Purpose** (what it is for), **Intuition** (the financial/statistical/algorithmic idea behind it), and **Code intuition** (the specific trick or invariant the implementation relies on). This mirrors the "explain *why*, not *what*" rule the code comments themselves follow.

### `core/validation.ts` — one shared result shape

```ts
type ValidationResult<T> =
  | { ok: true; value: T }
  | { ok: false; errors: ValidationError[] }
```

* **Purpose:** every validator in the app (dataset, config, and later the runner itself) needs to say "this is fine, here is the checked value" or "here is exactly what is wrong," without throwing.
* **Intuition:** the README's rule "never silently clip invalid values into plausible results" only works if invalid input has somewhere safe to go. A discriminated union makes the two outcomes mutually exclusive at the type level: code that reads `result.value` cannot compile unless it first narrowed `result.ok`.
* **Code intuition:** because the failure branch (`{ ok: false; errors }`) does not mention `T`, the exact same type and the exact same failure-handling code works for a `ValidationResult<AlignedDataset>`, a `ValidationResult<SimulationConfig>`, and later a `ValidationResult<SimulationResult>` — `runSimulation` reuses `validateSimulationConfig`'s failure branch completely unchanged (see below).

### `core/data/datasetTypes.ts` — the aligned dataset contract

Key exports: `Frequency`, `BaseCurrency`, `DatasetIdentity`, `AlignedDataset`, `validateAlignedDataset`.

* **Purpose:** describe "one common history" — one date vector plus one return column per asset, one inflation column, one risk-free column — all sharing the same row count, so row `t` always means the same date everywhere.
* **Intuition:** the bootstrap engine's entire statistical validity rests on sampling *complete rows*. That is only meaningful if every column has already been aligned to the same calendar and every row is fully observed — this type is where that guarantee is stated and enforced, once, instead of re-checked by every consumer.
* **Code intuition:** `Frequency`/`BaseCurrency` are string literal unions (`'weekly' | 'monthly'`, `'USD' | 'EUR'`), not `string`, so an unsupported value is a compile error, not a runtime surprise. `validateAlignedDataset` checks matching dimensions, strictly increasing ISO dates, finite values, a `-100%` return floor, and the 60-month/260-week minimum history — all as structured `ValidationError`s rather than thrown exceptions or clipped values.

### `core/simulation/simulationTypes.ts` — configuration, scenario, and result contracts

Key exports: `CashFlowConfig`, `SimulationConfig`, `PeriodScenario`, `SimulationResult`, `validateSimulationConfig`.

* **Purpose:** define the shape of a run *before* any engine exists — what the user configured, what one sampled period looks like, and what a finished run reports.
* **Intuition:** `CashFlowConfig` is a discriminated union (`lumpSum` / `dca` / `valueAveraging`) instead of a bag of optional fields, because the README defines these as mutually exclusive modes with different required data (DCA needs an amount; value averaging needs a target increase; lump sum needs nothing else). Making invalid combinations unrepresentable is cheaper than validating them at runtime.
* **Code intuition:** `PeriodScenario` is the one shape every engine must produce (`assetReturns`, `inflation`, `riskFreeRate`, `sourceRowIndex`) — it deliberately contains nothing about holdings, weights, or contributions, which is what lets `runSimulation` stay engine-agnostic. `validateSimulationConfig` takes `assetCount` as a parameter rather than reading `config.weights.length`, so a config whose weight array accidentally has the wrong length is *caught*, not silently compared against itself.

### `core/simulation/simulationEngine.ts` — the one extension boundary

```ts
type SimulationEngine = { nextScenario(): PeriodScenario }
```

* **Purpose:** this is the entire contract a simulation method must satisfy to plug into the unchanged portfolio-accounting loop.
* **Intuition:** the README requires that a new engine "must not implement contributions, rebalancing, taxes, leverage, charts, or exports." Making the interface this narrow is what makes that rule enforceable — there is nothing wider to accidentally implement.
* **Code intuition:** an engine only ever returns a scenario; it never receives holdings or config. This is why the historical bootstrap engine (below) and the future Student's $t$ engine can be swapped without touching `runSimulation` or `stepPortfolioPeriod` at all.

### `core/math/random.ts` — deterministic `xoshiro128**`

Key exports: `XOSHIRO128_STAR_STAR_VERSION`, `createXoshiro128StarStar(seed)` → `{ nextUint32, nextUniform, nextInt }`.

* **Purpose:** every "random" choice in the app (which historical row to sample) must be exactly reproducible from one 32-bit seed, and must never call `Math.random()`.
* **Intuition:** reproducibility is a product requirement, not a nice-to-have — the README requires the seed, PRNG version, and full config in every export so a result can be regenerated exactly. `xoshiro128**` is a small, fast, well-studied generator that fits this without pulling in a dependency for "a short deterministic algorithm that is easier to test locally" (the Maintenance Policy's own words).
* **Code intuition:**
  * `expandSeed` turns one 32-bit seed into four state words using a SplitMix32-style rule: add the golden-gamma constant `0x9e3779b9`, then run it through two MurmurHash3 finalizer mixes (`0x85ebca6b`, then `0xc2b2ae35`). This exists because xoshiro's own state update is weak at scrambling a *single* seed value into four *independent-looking* words — SplitMix32 is a cheap, well-known way to do that expansion, and it is versioned (`XOSHIRO128_STAR_STAR_VERSION`) so a future change to this rule cannot silently change every past seed's sequence.
  * Every intermediate value is masked with `>>> 0` after each operation. JavaScript's bitwise operators otherwise produce *signed* 32-bit integers, which would silently corrupt the generator's internal state the first time a word's high bit was set.
  * `nextInt(upperExclusive)` uses **rejection sampling before the modulo**: it computes the largest multiple of `upperExclusive` that fits under `2^32` and redraws whenever `nextUint32()` lands above that cutoff. Without this, rows near the start of the table would be drawn very slightly more often than rows near the end whenever `upperExclusive` does not divide `2^32` evenly — for six historical rows that bias is tiny per draw, but it is exactly the kind of silent, compounding error a Monte Carlo engine cannot tolerate.
  * The all-zero expanded state is explicitly rejected, because xoshiro's XOR/shift update is a fixed point at all-zero: it would generate an infinite stream of zeros.

### `core/simulation/historicalBootstrap.ts` — historical joint bootstrap engine

`createHistoricalBootstrapEngine(dataset, seed) → SimulationEngine`

* **Purpose:** implement the README's "Historical Joint Bootstrap" — sample *complete* historical rows with replacement, never sample each asset independently.
* **Intuition:** the whole point of a joint bootstrap is preserving contemporaneous cross-asset dependence — if two assets crashed together in the same real week, an independent-column sampler could recombine that week's stock return with an unrelated week's bond return and manufacture a diversification benefit that never actually existed historically. Sampling one row index and reading every column at that same index is what prevents that.
* **Code intuition:** `nextScenario()` calls `random.nextInt(dataset.dates.length)` exactly once per call, then reads `dataset.assetReturns[i][sourceRowIndex]` for every asset, plus `dataset.inflation[sourceRowIndex]` and `dataset.riskFreeRates[sourceRowIndex]`, from that one index. Sampling *with* replacement (the same row can be drawn twice, even consecutively) is deliberate: it treats each historical week as one independent draw from the portfolio's true (unknown) return distribution, which is the entire statistical premise of a bootstrap. A fresh plain `number[]` is returned each call — never a typed-array view into the dataset — so nothing downstream can accidentally mutate the historical source data.

### `core/portfolio/cashFlows.ts` — minimal unleveraged portfolio accounting

Key exports: `allocateInitialInvestment`, `applyPeriodReturn`, `computeScheduledContribution`, `investContribution`, `stepPortfolioPeriod`.

* **Purpose:** turn one `PeriodScenario` plus the previous period's holdings into the next period's holdings — the smallest possible accounting step, with no rebalancing, taxes, costs, or leverage yet.
* **Intuition — timing matters:** the README's per-period operation order is "apply returns, *then* add the contribution." A DCA contribution added at the end of the period should not retroactively earn that period's market return — real brokerage cash arriving on a date does not travel back in time. Getting this ordering backwards is a common and very easy Monte-Carlo bug, so it is enforced in one function (`stepPortfolioPeriod`) rather than left to be repeated correctly by every caller.
* **Code intuition, function by function:**
  * `allocateInitialInvestment(initialInvestment, weights)` computes `initialInvestment * weights[i]` — the only place the lump sum is split across assets. Every later contribution reuses this same target-weight split.
  * `applyPeriodReturn(holdings, assetReturns)` computes `holdings[i] * (1 + assetReturns[i])` — one asset at a time, with no notion of a portfolio total. This is why it is correct regardless of which engine produced the returns.
  * `computeScheduledContribution(cashFlow, periodIndex, initialInvestment, preContributionEquity)` switches on the `CashFlowConfig` union: `0` for lump sum, the fixed `amount` for DCA, and for value averaging, $C_t=\max(0, A_0 + t\Delta - E_t^-)$ where $A_0 + t\Delta$ is the target path and $E_t^-$ is equity *before* this period's contribution. The `Math.max(0, ...)` is the entire mechanism that stops value averaging from ever describing a withdrawal — the README requires "never withdraw when equity exceeds the target," and this clamp is precisely why that can never happen: a negative target-shortfall becomes `0`, not a negative contribution.
  * `investContribution(holdings, weights, contribution)` computes `holdings[i] + weights[i] * contribution` — the contribution is spent at target weights, but *existing* holdings are left exactly where the return step put them. This is a deliberate limitation: weights are allowed to drift between contributions because rebalancing does not exist yet (a later phase adds it). A test proves this drift happens on purpose.
  * `stepPortfolioPeriod(...)` is the orchestrator: record `startEquity`, apply the return, compute `preContributionEquity`, compute and invest the contribution, then return `{ holdings, contribution, equity, neutralReturn }`. `neutralReturn = preContributionEquity / startEquity − 1` is the return caused by *markets alone*, before any external cash arrived — this is the number a future volatility/Sharpe calculation must use, precisely because mixing in contribution timing would make a $100 DCA top-up look like investment performance.
  * Every function here takes and returns plain `number[]`, never mutates its arguments, and never touches taxes, costs, or debt — so wrapping this function later (for taxes, leverage, etc.) cannot change what it already does.

### `core/math/quantiles.ts` — one canonical percentile rule

Key exports: `QUANTILE_VERSION`, `QUANTILE_LEVELS`, `REPRESENTATIVE_PATH_QUANTILE_LEVELS`, `computeQuantile`, `computeQuantileSeries`.

* **Purpose:** there is more than one textbook definition of "percentile." The README requires picking exactly one and using it everywhere, so a chart line labelled "p50" and a future metrics-table "p50" number always agree.
* **Intuition:** `computeQuantile(sortedAscendingValues, q)` implements linear interpolation between order statistics — $h=(n-1)q$, then interpolate between the values at $\lfloor h\rfloor$ and $\lceil h\rceil$. This is the same method Excel calls `PERCENTILE.INC` and NumPy calls its default `'linear'` method, chosen specifically so a hand-checked spreadsheet number and this app's own number agree exactly, not approximately.
* **Code intuition:** `QUANTILE_LEVELS` (`0.1, 0.25, 0.5, 0.75, 0.9`) is the five-level set the README's Key Metrics Panel reports terminal wealth at. `REPRESENTATIVE_PATH_QUANTILE_LEVELS` (`0.01, 0.1, 0.25, 0.5, 0.75, 0.9, 0.99`) is a deliberately separate, wider list `runSimulation.ts`'s `selectRepresentativePaths` uses to pick which real simulated paths the chart draws — kept apart so the chart's own scope (which now includes tail markers) doesn't silently grow the metrics table's scope too. `computeQuantileSeries` calls `computeQuantile` five times per period (`p10`…`p90`) over the **cross-section of every path's value at that period** — not over one path's own history over time — and remains a small, independently tested, correct utility, but `runSimulation` no longer calls it: see that file's entry below and LOG.MD for why a per-period cross-sectional aggregate was replaced with real representative paths as the chart's data source. `QUANTILE_VERSION` exists for the same reason `XOSHIRO128_STAR_STAR_VERSION` and `HISTORICAL_BOOTSTRAP_MODEL_VERSION` exist: so a stored result can say exactly which rule produced its numbers if the method is ever revisited.

### `core/simulation/runSimulation.ts` — the headless runner

`runSimulation({ engine, dataset, config, modelVersion, prngVersion }) → ValidationResult<SimulationResult>`

* **Purpose:** this is the first place every earlier Phase 1 piece runs together — validated dataset, seeded PRNG, an engine, and `stepPortfolioPeriod` — driven in a `paths × periods` loop and reduced into a chart-ready summary.
* **Intuition:** the function deliberately knows as little as possible. It receives an **already-constructed** engine rather than building one, so it stays usable by every future engine unchanged — Phase 3's Student's $t$ engine only needs to be constructed and handed in, exactly like the bootstrap engine is today. This is the practical payoff of the `SimulationEngine` boundary above.
* **Code intuition:**
  * Per-period equity for every path lives in one flat `Float64Array` of length `(periods + 1) * paths`, indexed as `period * paths + pathIndex`, instead of a nested `number[][]`. A flat typed array stores raw 8-byte numbers with no per-element object overhead — the same "typed arrays for large simulation data" principle the README's Main Goals section commits to — and the existing `paths × periods ≤ 10,000,000` work budget already caps this at roughly 80&nbsp;MB.
  * Paths run one at a time, period by period, calling `engine.nextScenario()` and `stepPortfolioPeriod` completely unmodified. This means the engine's internal PRNG stream advances in **path-major, period-minor order** — all of path 0, then all of path 1, and so on — and that ordering is now part of a run's reproducible identity, alongside the seed.
  * Only `equity` is checked for `Number.isFinite(...)` — not `neutralReturn`. Under the validated dataset's $-100\%$ return floor and long-only accounting, equity can hit exactly `0` (a total wipeout) but cannot go negative or non-finite; a wiped-out path can still legitimately compute a meaningless `0/0` `neutralReturn` later, and that belongs to a future volatility phase, not this one. When equity does fail, every remaining period for that path is explicitly written as `NaN`, never left at a typed array's default `0` — because a real wipeout (`$0`) and "this path stopped being simulated" (`NaN`) are different facts, and downstream consumers (`terminalWealth`, `selectRepresentativePaths`) must be able to tell them apart.
  * Retained paths (full period-by-period detail, for a future UI to let a user click through) are always the first `min(50, paths)` paths **by index** — never random, never picked to match a percentile. Every path is an equally valid independent draw, so a fixed prefix is not a biased sample, and it needs no extra PRNG draw or tie-breaking rule to be exactly reproducible.
  * **`selectRepresentativePaths` is what the chart actually renders.** For each level in `REPRESENTATIVE_PATH_QUANTILE_LEVELS`, it sorts every path's finite terminal wealth once, computes that level's target value with the same canonical `computeQuantile`, then picks the *actual simulated path* whose own terminal wealth lands nearest that target (ties resolve to the lower path index) and returns that path's full trajectory. This is a real, single simulated future — not an aggregate statistic recomputed independently at every period — which is why two representative paths can cross at intermediate periods even though they never cross at the terminal one; see LOG.MD for a concrete measurement of how often this happens against the real dataset. If every path failed, there is no terminal-wealth distribution to pick from and this returns an empty list, not a fabricated pick. Cost is `O(paths log paths)` once plus `O(levelCount × paths)` to find the nearest path per level — far cheaper than the `O(periods × paths log paths)` a full per-period cross-sectional sort (`computeQuantileSeries`) would have cost, which is the other reason this replaced it here.
  * The result's `metadata.algorithms` combines the caller-supplied `modelVersion`/`prngVersion` (this file cannot know which engine produced the scenarios) with this file's own `QUANTILE_VERSION` — so every result can answer "exactly which data, seed, model, and quantile rule produced this number," including the rule behind each representative path's selection.

### Phase 1.6 — independent cross-check

`validation/phase_1_6_independent_check.py` reimplements `xoshiro128**` and the cash-flow accounting rules **from this README's prose alone**, in Python, without reading the TypeScript line-by-line. It prints a period-by-period trace (sampled row, holdings, contribution, neutral return, equity) for one lump-sum, one DCA, and one value-averaging run. Those numbers are pasted as expected values into `src/core/simulation/independentCheck.test.ts`, which drives the real `createHistoricalBootstrapEngine` and `stepPortfolioPeriod`/`runSimulation` against the same seeds and dataset and asserts an exact match.

* **Intuition:** a hand-calculated test written by the same person who wrote the implementation can encode the same mistake twice — the test "passes" because the bug is symmetric. A second implementation, in a second language, derived only from the specification, does not share that risk.
* **Code intuition:** the Python script rounds every literal return through an actual 32-bit float (`struct.pack('f', ...)` then unpack) before using it in `float64` arithmetic. This matters because the dataset's returns are stored as `Float32` on disk — "a transport format, not the accounting precision," per the Dataset Format rules — so the *correct* independent ground truth is what double-precision arithmetic produces starting from a float32-rounded input, not from the unrounded decimal literal. Skipping this step would make the cross-check fail on real, expected floating-point rounding rather than catch an actual bug.

## Work-in-Progress Plan

This is a checklist tracking near-term execution, not a spec. It refines steps 1–5 of "Implementation Order" into a stricter test-before-UI sequence — same scope, different order of proof — and defers to "Implementation Order" for everything after. Check items off as they land; do not change scope here without updating the sections above.

- [x] **Phase 0 — Minimal frontend shell.** Vite app, the three routes, a single **Run** button, and a raw JSON/number dump of the result. No real input form. Purpose: a harness to exercise the backend, not a usable product.
- [x] **Phase 1 — Bootstrap engine + portfolio accounting, headless.** Build the following in `core/` only, with no dataset fetch, binary parsing, worker, chart, or real input UI. Keep scenario generation separate from portfolio accounting so later engines reuse the same accounting loop.
  - [x] **Phase 1.1 — Core contracts and validation.** Add `core/data/datasetTypes.ts`, `core/simulation/simulationTypes.ts`, and `core/simulation/simulationEngine.ts`. This subphase defines the small, framework-independent language that the bootstrap engine, portfolio loop, worker, and future engines share. It adds no loader, binary parser, worker, React state, or financial calculation.
    1. **Define closed scalar types and dataset identity.** Represent frequency as `'weekly' | 'monthly'`, base currency as `'USD' | 'EUR'`, and cash-flow mode as a discriminated union rather than strings scattered through code. Define one dataset-identity value containing the dataset version, checksum, frequency, and base currency. Use ISO calendar-date strings for the ordered date vector. This makes unsupported modes unrepresentable at compile time and ensures every later result can identify exactly which released data it used.
    2. **Define the aligned in-memory dataset.** Represent selected asset identifiers, dates, one periodic simple-return column per selected asset, one inflation column, and one base risk-free-rate column. The arrays must share one row count and row $t$ must refer to the same date in every column. The eventual binary artifact remains little-endian, column-major `Float32`; this runtime type describes data after loading and selection, where reading a typed-array value yields a JavaScript `number` for accounting. Keeping the transport representation outside this contract lets core tests use small deterministic fixtures without fetching files or depending on browser APIs.
    3. **Define the scenario and engine boundary.** `PeriodScenario` contains one complete sampled row: asset returns in selected-asset order, inflation, risk-free rate, and source row index. `SimulationEngine` only produces the next scenario; it does not receive holdings, contributions, taxes, or React state. This is the single extension boundary: bootstrap now and Student's $t$ later can feed identical scenarios into unchanged accounting, which prevents duplicated financial rules.
    4. **Define immutable run inputs and outputs.** Model portfolio weights, initial investment, path count, period count, seed, and a discriminated cash-flow configuration: lump sum has no later amount, DCA has a non-negative periodic amount, and value averaging has a non-negative target increase. Define result and failure types that preserve the full configuration, dataset identity, seed, and algorithm-version fields. Inputs are read-only and functions return new values, so one simulated path cannot mutate another path's configuration or historical data.
    5. **Implement two explicit validation stages.** First validate dataset structure: supported frequency/currency, unique asset identifiers, matching nonzero dimensions, strictly increasing valid dates, finite values in all retained columns, returns no lower than $-100\%$, at most six assets, and at least 60 monthly or 260 weekly common rows. Then validate a run configuration: finite non-negative amounts, weights summing to one within 0.01 percentage points, unsigned 32-bit integer seed, positive integer paths and periods, 30-year horizon, 50,000-path maximum, and $N\times T\le10{,}000{,}000$. Return structured validation errors for expected bad input instead of clipping values, silently dropping rows, or relying on exceptions; the later UI can display those same errors beside controls.
    6. **Prove the contracts before building the engine.** Add focused tests for valid datasets, each rejected invariant, exact boundary values such as 60/260 observations and a $-100\%$ return, all cash-flow union variants, and the work-budget boundary. Use direct one-period `PeriodScenario` fixtures for hand-calculated accounting tests; use generated 60- or 260-row fixtures when testing full-run eligibility. This proves that the test harness respects production history requirements without making small financial examples unreadable.
  - [x] **Phase 1.2 — Versioned deterministic randomness.** Add `core/math/random.ts` with a local `xoshiro128**` generator. Expand one 32-bit seed into four state words with one documented, versioned rule; reject the all-zero state; expose unsigned-32-bit, $[0,1)$ uniform, and unbiased bounded-integer draws; never call `Math.random()`. Test fixed reference vectors, same-seed reproducibility, different-seed divergence, output bounds, and row-index bounds. Keep this module independent of simulation and React code.
  - [x] **Phase 1.3 — Historical joint bootstrap engine.** Add `core/simulation/historicalBootstrap.ts` and `core/simulation/historicalBootstrap.test.ts`. Implement the existing `SimulationEngine` boundary only: construct an engine from one already validated `AlignedDataset` and a validated unsigned 32-bit seed, create one local `xoshiro128**` generator, and return one complete `PeriodScenario` on every `nextScenario()` call. This subphase introduces no portfolio state, cash-flow logic, results, dataset loader, binary parsing, worker, React code, or mutable global randomness.
    1. **Define the construction boundary.** Export one plainly named factory, such as `createHistoricalBootstrapEngine(dataset, seed)`, returning `SimulationEngine`. The caller is responsible for running `validateAlignedDataset` before construction, so the engine can rely on matching, finite columns and a nonzero row count. It must neither modify the dataset's typed arrays nor retain or expose a mutable selected-row buffer.
    2. **Draw complete rows with replacement.** Each `nextScenario()` call must call `createXoshiro128StarStar(seed).nextInt(dataset.dates.length)` exactly once through the engine's private generator. The resulting row index is eligible again on the next call, including an immediate repeat. Sampling must use the PRNG's rejection-sampled bounded integer rather than `Math.random()`, modulo arithmetic, shuffled cycles, or an independent generator per asset.
    3. **Preserve the joint observation.** Read every asset return at the single selected row in `dataset.assetReturns`, preserving `dataset.assetIds` order. Read `dataset.inflation[rowIndex]` and `dataset.riskFreeRates[rowIndex]` from that same row, then return `{ assetReturns, inflation, riskFreeRate, sourceRowIndex: rowIndex }`. The engine samples complete historical outcomes, so correlations and joint stress events remain intact; it intentionally does not preserve serial dependence, volatility clustering, or crisis duration across independently sampled periods.
    4. **Keep the scenario isolated from the dataset.** Return a new ordinary number array for `assetReturns` on each call rather than a typed-array view or a reused mutable array. Values are JavaScript `number`s for later accounting. Do not include dates, asset IDs, holdings, weights, contributions, debt, taxes, or any future engine-specific fields in `PeriodScenario`; row identity is represented solely by `sourceRowIndex`.
    5. **Define deterministic model identity.** Export a model-version constant, for example `HISTORICAL_BOOTSTRAP_MODEL_VERSION = 'historical-bootstrap-v1'`. Phase 1.5 will combine it with `XOSHIRO128_STAR_STAR_VERSION` and the later quantile version in result metadata. Changing the row-selection rule, return ordering, or scenario construction requires a version update and new fixed-seed reference expectations.
    6. **Prove behavior with focused fixtures.** Build small deterministic aligned-dataset fixtures locally in the test file; construction tests do not need to meet the 60/260-observation production-history rule because dataset validation is already covered in Phase 1.1. Test a fixed seed against an explicitly written source-row sequence, then verify every returned asset return, inflation value, and risk-free rate equals the corresponding source column at each returned `sourceRowIndex`. Use distinctive per-row values so independent-column sampling cannot accidentally pass.
    7. **Prove repeatability and distribution.** Create two engines with the same dataset and seed and assert that a fixed number of scenarios are identical, including row metadata; assert a different seed diverges over a sufficiently long fixed sequence. For a small known row count, collect a predeclared fixed number of draws and assert each row frequency remains inside a predeclared tolerance around the expected count. This is a regression guard for the bounded-draw path, not evidence that a finite sample is perfectly uniform.
    8. **Completion check.** Run the focused bootstrap test, then the complete Vitest suite, lint, formatting, and production build. The only new production dependency must be the existing `AlignedDataset`, `SimulationEngine`, `PeriodScenario`, and local xoshiro PRNG modules.
  - [x] **Phase 1.4 — Minimal unleveraged portfolio accounting.** Add `core/portfolio/cashFlows.ts` and its colocated test file. Implement the smallest pure accounting step needed by the future runner: turn one `PeriodScenario` plus the previous period's holdings into the next period's holdings. This subphase introduces no rebalancing, taxes, transaction costs, debt/leverage, benchmark handling, result aggregation, or React/worker code; those stay in later phases.
    1. **Define the per-period result shape.** Export a `PortfolioPeriodResult` type holding `holdings` (one number per asset, in `assetIds` order), `contribution` (the actual external cash added this period, always `0` for lump sum), `equity` (sum of holdings after the contribution), and `neutralReturn` (the portfolio return caused by asset returns alone, before the contribution is added). Every field is a plain `number`/`number[]`, matching the "convert to JavaScript `number` for accounting" rule; nothing here is a `Float32Array`.
    2. **Implement `allocateInitialInvestment(initialInvestment, weights)`.** Return `holdings[i] = initialInvestment * weights[i]`, the period-zero starting point before any scenario is applied. This is the only place the initial lump sum is split across assets; every later period only ever invests new contributions this same way.
    3. **Implement `applyPeriodReturn(holdings, assetReturns)`.** Return a new array with `holdings[i] * (1 + assetReturns[i])`. This is a pure per-asset simple-return compounding step with no cross-asset interaction, so it is the same function regardless of which engine produced the scenario.
    4. **Implement `computeScheduledContribution(cashFlow, periodIndex, initialInvestment, preContributionEquity)`.** Switch on the `CashFlowConfig` union: `lumpSum` always returns `0`; `dca` always returns `cashFlow.amount`; `valueAveraging` computes the target path value $A_t = \text{initialInvestment} + \text{periodIndex} \times \Delta$ and returns $C_t=\max(0, A_t - \text{preContributionEquity})$, so the function can never return a negative number and therefore never models a withdrawal. `preContributionEquity` is `sum(applyPeriodReturn(...))`, i.e. $E_t^-$ from the README's Portfolio Logic section, computed by the caller before this function runs.
    5. **Implement `investContribution(holdings, weights, contribution)`.** Return `holdings[i] + weights[i] * contribution`, spending the whole contribution at target weights and leaving every other unit of value exactly where the return step left it. Existing holdings are never resized to close weight drift; that is deliberate until a later rebalancing phase.
    6. **Implement `stepPortfolioPeriod(holdings, scenario, cashFlow, weights, periodIndex, initialInvestment)`.** Orchestrate steps 3–5 in the exact order required by the README's per-period operation list: (a) record `startEquity = sum(holdings)`; (b) call `applyPeriodReturn` to grow holdings with `scenario.assetReturns`; (c) compute `preContributionEquity = sum(grownHoldings)`; (d) call `computeScheduledContribution`; (e) call `investContribution` to get the final holdings; (f) compute `equity = sum(finalHoldings)` and `neutralReturn = preContributionEquity / startEquity - 1`. Return one `PortfolioPeriodResult`. This function never reads or writes taxes, debt, transaction costs, or rebalancing bands, so later phases can wrap it without changing its behavior.
    7. **Keep every function pure and dependency-free.** No function in this file may mutate its `holdings` or `weights` argument, read a `PeriodScenario` field the engine does not define, or depend on `paths`/`periods` from `SimulationConfig`. `core/simulation/runSimulation.ts` (Phase 1.5) is the only caller that will loop this function across periods and paths.
    8. **Prove behavior with hand-calculated tests.** Cover: one-asset lump-sum compounding against a manually computed value; a two-asset case where each asset gets a different return, checked against a hand-summed equity; that a DCA contribution is added after the period's return is applied (so it earns zero return in the period it arrives); that repeated unequal per-asset returns without rebalancing drift the weights away from target over several periods; that value averaging never contributes a negative amount once the path value already exceeds the target; and that, with no costs or taxes yet in the model, `equity` after each period exactly equals `startEquity * (1 + neutralReturn) + contribution` (value conservation).
  - [x] **Phase 1.5 — Headless runner and quantiles.** Add `core/math/quantiles.ts` and `core/simulation/runSimulation.ts`. This subphase is the first place that ties every earlier Phase 1 piece together: the validated dataset (1.1), the seeded PRNG (1.2), the bootstrap engine (1.3), and the per-period accounting step (1.4) are driven in a loop and reduced into the small, chart-ready summary a future UI will render. It still adds no dataset fetch, binary parsing, worker, chart, or React code — those are Phase 2.
    1. **Decide what the runner is allowed to know.** `runSimulation` must stay usable by every future engine, not just bootstrap, so it may only depend on the existing `SimulationEngine` interface, the already-validated `AlignedDataset`, and a `SimulationConfig`. It receives an **already-constructed** engine (for example, one built by `createHistoricalBootstrapEngine(dataset, seed)`) rather than building one itself. *Intuition:* the runner's job is "loop and account," not "know how to sample history." *Consequence:* Phase 3's Student's $t$ engine reuses this file completely unchanged — it only needs to be constructed and handed in. *Limitation:* the caller (a small helper today, the worker in Phase 2) is responsible for using the *same* seed and model to build both the engine and the version strings passed alongside it; nothing in the type system forces that pairing, so this is a documented convention, not a compiler-enforced guarantee.
    2. **Reuse the existing config validator instead of re-deriving limits.** Before running anything, call `validateSimulationConfig(config, dataset.assetIds.length, dataset.identity.frequency)` — the same function Phase 1.1 already tested for the 50,000-path maximum and the $N\times T\le10{,}000{,}000$ work budget. Take `assetCount` from `dataset.assetIds.length` rather than from `config.weights.length`: if those two numbers ever disagree (a caller built a five-weight config against a six-asset dataset), reading the count from the dataset is what lets the existing "weights count must match asset count" check actually catch the mistake, instead of comparing a number to itself. *Advantage:* zero new validation logic, and the 1.1 test suite already covers the boundary cases. *Consequence:* `runSimulation` returns the same `ValidationResult<T>` shape (`{ ok: true, value }` / `{ ok: false, errors }`) already used everywhere else, just with `T = SimulationResult` this time — the failure branch needs no new code because `ValidationResult`'s `ok: false` case never depended on `T` in the first place. This is the payoff of that Phase 1.1 design choice.
    3. **Store per-period equity in one flat typed array, not nested arrays.** Allocate a single `Float64Array` of length `(periods + 1) * paths` and write each path's equity into it at `period * paths + pathIndex`. *Intuition:* a plain JavaScript `number[][]` (array of arrays) stores every number as a separately-allocated boxed object with pointer overhead; a flat `Float64Array` stores raw 8-byte numbers contiguously, which is the same "typed arrays for large simulation data" principle the README's Main Goals section already commits to. *Consequence:* worst-case memory is bounded and predictable — the work budget already caps `paths * periods` at 10,000,000, so this buffer never exceeds roughly `10,000,000 * 8 bytes ≈ 80 MB` plus one extra row of `paths` values for period zero. *Limitation:* 80 MB is still a lot for a browser tab; Phase 2's worker will need to watch actual memory behavior at the real maximum, and a future phase could stream quantiles period-by-period instead of buffering everything if that turns out to matter. This phase deliberately does not solve that; it only keeps the current approach inside the already-agreed budget.
    4. **Run one path at a time, period by period, calling `stepPortfolioPeriod` unchanged.** For each path: start from `allocateInitialInvestment`, record that as period zero's equity, then for periods `1..periods` call `engine.nextScenario()` once and feed it straight into `stepPortfolioPeriod` from Phase 1.4 with no modification. *Intuition:* this is the "single extension boundary" promise made all the way back in Phase 1.1 — the accounting function does not know or care that its scenario came from a bootstrap draw. *Consequence:* the engine's internal PRNG stream advances once per `nextScenario()` call, in path-major then period-minor order (all of path 0's periods, then all of path 1's periods, and so on); this ordering is now part of the run's deterministic identity, alongside the seed. *Future problem to flag:* if a later change ever wants to run paths in parallel (for example, splitting work across several Web Workers in a future performance phase), the paths can no longer share one engine instance advancing a single stream — each worker would need its own independently-seeded engine, and the exact scenario sequence would change. That tradeoff is intentionally deferred; Phase 1.5 optimizes for one clear, provably reproducible sequence, not for parallelism.
    5. **Only treat non-finite equity as a path failure — not `neutralReturn`.** After each period, check `Number.isFinite(result.equity)`. If it fails, record one `SimulationFailure { pathIndex, periodIndex, code, message }`, stop simulating further periods for that path, and fill its remaining period slots in the flat buffer with `NaN` (never `0`). *Intuition:* under the validated dataset's rules (returns floored at $-100\%$) and Phase 1.4's long-only accounting, equity can reach exactly zero (a full wipeout) but cannot go negative or non-finite — so this check is a defensive contract for engines this phase does not control, not a condition expected to fire under bootstrap sampling of real data. *Why not guard `neutralReturn` too:* a fully wiped-out, lump-sum, single-asset path can legitimately produce a `0 / 0` `neutralReturn` in a later period even though its equity (a real, meaningful $0) is perfectly finite; `neutralReturn` belongs to a future volatility/Sharpe phase that will define its own handling for that case when it is built. Scoping the guard to just what Phase 1.5 actually consumes (equity) avoids solving a problem this phase does not have yet. *Consequence:* quantile computation (step 6) must explicitly skip `NaN` entries rather than let them silently count as zero-valued observations, which is what a naive flat-buffer default would otherwise produce. *Limitation:* this failure path is expected to be dormant in ordinary bootstrap runs against validated data; it is proven with a deliberately malformed hand-built fake engine in tests, not with real historical data, precisely because real data cannot trigger it today.
    6. **Compute one canonical quantile function and reuse it everywhere.** In `core/math/quantiles.ts`, implement `computeQuantile(sortedAscendingValues, q)` using linear interpolation between order statistics: $h=(n-1)q$, then interpolate between the values at $\lfloor h\rfloor$ and $\lceil h\rceil$. This is the same method spreadsheets call `PERCENTILE.INC` and NumPy calls its default `linear` method, chosen specifically so a hand-checked spreadsheet value and the app's own number agree exactly. *Intuition:* "percentile" has more than one textbook definition (there are at least nine named methods in common statistics references); the README requires picking exactly one and using it everywhere so that, for example, the p50 line on a future chart always matches the p50 number in the metrics table. *Consequence:* export a single `QUANTILE_VERSION` constant next to the function, so if this method is ever changed, every stored or exported result can say precisely which rule produced it — mirroring how `HISTORICAL_BOOTSTRAP_MODEL_VERSION` and `XOSHIRO128_STAR_STAR_VERSION` already work. *Limitation:* linear interpolation between order statistics is a smooth, well-behaved estimator for large samples, but with very few surviving (finite) observations at a period — for example, if almost every path has already failed by then — a "10th percentile" computed from 3 numbers is not a statistically meaningful percentile; this phase computes the number honestly rather than hiding the small sample, but does not add a separate small-sample warning. That is a reasonable future addition once real failure rates are observed.
    7. **Compute the five period bands by aggregating *across paths*, not *across time*.** `computeQuantileSeries` takes one cross-sectional sample per period — "every path's equity at period $t$" — sorts it ascending, and calls `computeQuantile` five times for $q\in\{0.10,0.25,0.50,0.75,0.90\}$ to fill `QuantileSeries.p10..p90`, one `Float64Array` of length `periods + 1` each. *Intuition:* this is the axis that is easy to mix up — the band chart's p50 line is "the median outcome among all simulated futures at that point in time," not "the median value of one path's history." *Consequence:* this directly implements the README's Visualization rule that percentile bands are aggregate statistics and must never be treated as a selectable individual path. *Advantage:* keeping `computeQuantile`/`computeQuantileSeries` as small pure functions over plain arrays (not the runner's flat buffer) makes them trivially hand-testable with 3–5 element examples, independent of any simulation machinery.
    8. **Select retained full-detail paths deterministically by index, not by percentile or randomly.** Define `RETAINED_PATH_COUNT = 50` and always retain paths `0` through `min(50, paths) - 1`. For each retained path, store its full `values: Float64Array` (equity at every period) and its `scenarios: readonly PeriodScenario[]` (one per period, so a future UI can show which historical rows were sampled). *Intuition:* every path is an equally valid, independent draw — there is nothing statistically special about "path 7" versus "path 12,003" — so picking a fixed prefix is not a biased sample, and it is trivially deterministic and easy to test. *Advantage over alternatives:* selecting "the path closest to p50" or a random subset would need extra computation or an extra PRNG draw, and would make the retained set depend on details (like tie-breaking) that are harder to pin down exactly in a test. *Limitation, explicitly flagged for a future decision:* the retained, clickable paths a user inspects will almost never include the exact paths whose terminal wealth happens to sit at p10/p25/p50/p75/p90 — a user clicking through the 50 retained lines cannot assume any of them represents "the median outcome." If that turns out to matter for the product, a later phase could deliberately retain, say, the paths nearest each quantile in addition to (or instead of) the first 50; this phase does not do that, to keep selection simple and independent of the quantile computation.
    9. **Assemble the full `SimulationResult`, deferring version identity to the caller where this file cannot know it.** Return `{ ok: true, value }` where `value.metadata.config` is the full, unmodified input configuration; `value.metadata.dataset` is `dataset.identity` (version, checksum, frequency, base currency); `value.metadata.algorithms` combines a `modelVersion` and `prngVersion` supplied by the caller (because only the caller knows which engine and PRNG built the scenarios) with this file's own `QUANTILE_VERSION`; `terminalWealth` is every path's final equity (or `NaN` for a failed path); `quantiles` comes from step 7; `retainedPaths` comes from step 8; and `failures` lists every recorded `SimulationFailure`. *Consequence:* a downstream export or bug report can always answer "exactly which data, seed, model, and quantile rule produced this number," which is the README's explicit requirement for every result and export.
    10. **Prove it with tests that isolate each moving part.** For `quantiles.ts`: hand-checked linear-interpolation examples (including exact order statistics when $h$ is an integer, and interpolated values when it is not), the single-value case, and a full period-by-period `computeQuantileSeries` example small enough to check by hand. For `runSimulation.ts`: a tiny fixed dataset and fixed seed run through by hand (comparing every period's holdings, contribution, and equity, not just the terminal value); the retained-path count and index selection at `paths` below, at, and above 50; the work-budget and path-count rejections reusing `validateSimulationConfig`'s existing boundaries; and a deliberately broken fake `SimulationEngine` (returning a non-finite asset return) that proves a path failure is recorded, its remaining periods are `NaN` rather than `0`, and quantiles computed over the same period correctly ignore that `NaN` instead of treating it as a zero-wealth outcome.
  - [x] **Phase 1.6 — Proof and completion gate.** Colocate Vitest tests with every `core/` module. Cover one-asset compounding, all three cash-flow modes, aligned-row bootstrap behavior, seeded reproducibility, quantile interpolation, invalid inputs, and accounting invariants. Validate one small seeded simulation period by period against an independent spreadsheet or Python calculation, including sampled row, holdings, contribution, neutral return, and equity rather than terminal wealth alone. Run formatting, linting, strict TypeScript checking, unit/statistical tests, and a production build; document PRNG/model/quantile versions and results in `LOG.MD`. Phase 1 is complete only when these checks pass without using React or visual chart inspection.
- [x] **Dataset artifact gate — required before real data replaces the Phase 2 placeholder.** Built one reproducible USD weekly release through the offline Python pipeline: a schema-validated `assets.json` catalogue, `manifest.json`, and the base-currency return matrix, all under `pipeline/` and `public/data/` only — nothing in `src/` changed. `pipeline/metadata/fx_routes.csv` exists but is intentionally empty for this release (see step 2). EUR and monthly artifacts remain later work; so does the browser-side loader that turns these artifacts into an `AlignedDataset`.
  1. [x] **Curate the asset metadata catalogue source-of-truth.** `pipeline/build_assets_catalogue.py` turns `pipeline/etf_metadata.csv` into `pipeline/metadata/assets.csv` — one row per `assetId` with name (fetched live from Yahoo's chart-API `meta.longName`, cached in `pipeline/metadata/fund_names_cache.csv`), a coarse keyword-classified `assetClass`/`region`, provider, listing exchange/currency, `exposureCurrency`/`returnCurrency`/`fxTreatment`/`isCurrencyHedged`, and AUM (joined from `data/etf_universe.csv`). ISIN, domicile, category, distribution policy, replication method, and TER are left blank: this pass had no reliable per-fund source for them, and an invented value would be worse than an honest gap — a disclosed limitation, not a silent one.
  2. [x] **Resolve exposure currency — by scoping the universe, not by researching every foreign listing.** Rather than researching each Milan/XETRA/Paris/Amsterdam/Toronto listing's true underlying exposure, v1 is scoped to the 98 tickers in `pipeline/etf_metadata.csv` whose Yahoo-reported **quote currency** is already `USD` (`FUTURE`/`INDEX` instrument types excluded too — see the script for why). For a USD-quoted asset, `exposureCurrency = returnCurrency = USD` by construction and the FX conversion formula is the identity, so no per-fund research was needed to resolve it correctly. The 17 EUR-quoted and 1 CAD-quoted tickers already inventoried (including the confirmed `CSSPX.MI`-style USD-exposure-but-EUR-quoted cases) are deferred to the EUR release, where their FX routes will actually be exercised.
  3. [x] **Decide the v1 USD investable universe and proxy/backfill mappings.** 98 assets (86 ETF, 10 mutual fund, 2 equity). For the 9 tickers with a precomputed `_spliced.csv` (already-extended history), empirically identified the actual backfill proxy by correlating each spliced file's pre-original-inception segment against every other series in `data/` — every match came back at correlation 1.0000 (`EMLC`←`PREMX`, `GLD`←`GC=F`, `GSG`←`^SPGSCI`, `HYG`←`VWEHX`, `IEMG`←`VEIEX`, `PFF`←`PREFX`, `SGOV`←`BIL`, `VNQI`←`CSRSX`, `VNQ`←`VGSIX`), and recorded as each asset's proxy record in `assets.csv`. Caveat: Yahoo's current name for `PREFX` ("T. Rowe Price Tax-Efficient Equity") does not describe a preferred-securities strategy, even though its pre-2007 prices moved in exact lockstep with `PFF` — the ticker was very likely reused after a merger/conversion; the proxy relationship rests on the measured correlation, not on today's name.
  4. [x] **Build the FX-route table.** `pipeline/metadata/fx_routes.csv` was written with its header only and zero rows — every included asset is USD-quoted (step 2), so `quoteCurrency == baseCurrency == USD` for all 98 and the FX formula never needs to run. The schema is in place for the first non-USD-quoted asset a future release adds.
  5. [x] **Build the inflation and short-rate series.** `pipeline/build_release.py` converts `FRED_CPIAUCSL` monthly CPI into weekly log-inflation increments by splitting each month's log growth evenly across however many rows of the weekly date grid fall in that month (summing a month's weekly increments exactly reproduces that month's original log growth). For the risk-free rate, it uses `EFFR_CASH` — already a compounding daily-EFFR index — directly: its own weekly `pct_change()` **is** the weekly risk-free return, with no separate annualized-to-periodic conversion needed (documented in the manifest's `units.riskFreeRate`).
  6. [x] **Compute the aligned weekly USD return matrix — as one full union, not one 260-week-minimum intersection.** Every asset keeps its own available weekly history; the matrix's date axis is the **union** of all 98 assets' plus the risk-free series' weekly dates (2,429 weeks, 1980-01-06 to 2026-07-19), with `NaN` before an asset's own inception rather than a dropped row. The 260-week common-history minimum is enforced by `validateAlignedDataset` at **selection** time in the browser (whatever 1–6 assets a user actually picks), exactly as the Dataset Format section already specifies — collapsing the whole 98-asset universe down to its single newest member's inception (`ETH`, 2024) would have made nearly the entire release unusable for no reason. Spot-checked several plausible portfolios (e.g. `SPY`+`AGG`, `VOO`+`BND`+`VNQ`) at 800–1,150 common rows, comfortably above 260.
  7. [x] **Write the binary matrix and `manifest.json`.** `public/data/returns-weekly-usd.f32`: little-endian, column-major `Float32`, 2,429 rows × 100 columns (98 assets, alphabetically ordered, then `CPI_INFLATION`, then `RISK_FREE_RATE`), 971,600 bytes. `public/data/manifest.json` records `schemaVersion`, `datasetVersion` (`usd-weekly-v1`), a `sha256:` content checksum, frequency/base currency, the full ordered date vector, the ordered column list, per-column byte offsets, `dtype`/`byteOrder`/`layout`, units per column group, and source/provenance notes.
  8. [x] **Write `assets.json` from `assets.csv` + `fx_routes.csv`.** Each of the 98 records carries the identity/presentation fields from `assets.csv`, its resolved proxy record (or `null`), and a `history` block (`firstDate`/`lastDate`/`rowCount`/`meetsWeeklyMinimum`) computed directly from that asset's own finite observations in the matrix — so the future ETF picker can show real eligibility, not a guess.
  9. [x] **Validate the artifacts the same way the browser will.** `pipeline/validate_release.py` recomputes the matrix's SHA-256 from the actual file bytes (not the manifest's claim), recomputes byte length from `rowCount × columnCount × 4`, confirms every date is valid ISO and strictly increasing, confirms no value is `+/-Infinity` (`NaN` is the only allowed non-finite value), and cross-checks `assets.json`'s asset set and per-asset `history.rowCount` against `manifest.json`'s columns and the matrix's actual finite-value counts. All checks passed on the first full run.
  10. [x] **Record provenance and close the gate.** Checksum, dimensions, and universe size are documented in `LOG.MD`'s dataset-artifact-gate entry, including the scope decisions above (USD-quote-only universe, union-not-intersection date grid) and the known gaps (ISIN/domicile/category/TER left blank; `assetClass`/`region` are a disclosed keyword heuristic, not researched fact). Monthly and EUR artifacts remain explicitly out of scope for this pass.
- [x] **Phase 2 — Worker integration.** Move the bootstrap-only runner into the Web Worker: transferable buffers, run IDs, batching, progress, and cancellation. Connect its real output to uPlot on the Phase 0 shell. This proves the browser architecture end-to-end while only one engine depends on it, so integration problems surface early and cheaply. Phase 2 proves the *architecture* — worker plumbing, message protocol, buffer transfer, cancellation, and charting — not the production dataset. It runs against one small, clearly labelled placeholder `AlignedDataset` generated in-memory; the real versioned loader arrives with the separate, still-pending Dataset artifact gate, and nothing built in Phase 2 may assume the placeholder's shape is final.
  - [x] **Phase 2.1 — Placeholder dataset and message-protocol contracts.** Add a temporary fixture dataset and the framework-independent Worker message types, with no `self`, `postMessage`, or React dependency yet.
    1. **Generate one placeholder `AlignedDataset` deterministically, not by hand-typing rows.** Add `src/features/simulator/placeholderDataset.ts`, exporting `createPlaceholderDataset()`. Use the existing `createXoshiro128StarStar` generator with a fixed local seed to synthesize at least 260 weekly rows for two assets, one inflation column, and one risk-free column, satisfying `validateAlignedDataset` unchanged. Reusing the already-proven PRNG instead of writing 260 literal numbers keeps the fixture reviewable and regenerable, and reuses a module Phase 1 already tested rather than adding a second source of "randomness."
    2. **Label the placeholder unambiguously in code and in its version fields.** Give it a `DatasetIdentity` whose `version` string contains `placeholder`, so a `SimulationResult` produced during Phase 2 can never be confused with one produced by the real Dataset artifact gate release, including after export.
    3. **Define the Worker request message.** In `src/workers/workerMessages.ts`, define `RunRequestMessage { type: 'run', runId, dataset, config, modelVersion, prngVersion }` using the exact `AlignedDataset`/`SimulationConfig` types Phase 1 already defined — the worker boundary must not invent a second, parallel data shape.
    4. **Define the three Worker response messages.** `ProgressMessage { type: 'progress', runId, pathsCompleted, totalPaths }`, `ResultMessage { type: 'result', runId, result: SimulationResult }`, and `ErrorMessage { type: 'error', runId, errors: ValidationError[] }`. Reuse `ValidationResult`'s existing error shape for `ErrorMessage` rather than inventing a new one.
    5. **Define the stale-message rule as one pure function.** Export `isCurrentRun(activeRunId, messageRunId)`. A run ID is a plain string minted by the caller (for example `crypto.randomUUID()`) each time **Run** starts a new attempt; every later message compares against it. Keeping this as one pure, boolean function — not inline comparisons scattered through the hook — is what makes "ignore stale messages" independently testable.
    6. **Define the transfer-list rule as one pure function.** Export `buildTransferList(result: SimulationResult): ArrayBuffer[]`, collecting the `.buffer` of `terminalWealth`, every quantile `Float64Array`, and every retained path's `values`. `postMessage(message, transferList)` moves ownership of these buffers to the main thread instead of structured-cloning them, which matters once a run's flat equity buffer approaches the ~80 MB budget Phase 1.5 already computed.
    7. **Prove every rule with plain unit tests, no Worker or DOM.** `placeholderDataset.test.ts` asserts the generated dataset passes `validateAlignedDataset`, has at least 260 rows, and is reproducible across two calls (same seed, same output). `workerMessages.test.ts` asserts `isCurrentRun` accepts a matching ID and rejects every mismatch, and `buildTransferList` returns exactly the buffers backing the result's typed arrays (identity-checked with `===`, not value-checked).
  - [x] **Phase 2.2 — Batched, progress-reporting runner.** Extend `core/simulation/runSimulation.ts` additively so a caller can observe progress without changing any existing behavior.
    1. **Add two optional fields to `RunSimulationInput`.** `batchSize?: number` (paths per progress notification) and `onBatchComplete?: (pathsCompleted: number, totalPaths: number) => void`. Default `batchSize` to `paths` (one implicit batch) so every existing Phase 1 call site and test keeps its exact current behavior with zero code changes.
    2. **Call the hook from inside the existing single-pass loop, not a second loop.** After finishing path index `p`, if `(p + 1) % batchSize === 0 || p === paths - 1`, call `onBatchComplete(p + 1, paths)`. The engine's one PRNG stream must keep advancing exactly as it does today — batching is purely a notification cadence layered on the unchanged path-major loop, never a reason to construct a second engine or restart the stream, which would silently change every seed's results.
    3. **Prove the addition is behavior-preserving.** Re-run every existing `runSimulation.test.ts` case unmodified — they must still pass with no edits, proving the new fields are additive. Add one new test asserting `onBatchComplete` fires with the expected `(pathsCompleted, totalPaths)` pairs for a fixed `paths` and `batchSize`, including the final partial batch when `paths` does not divide evenly.
  - [x] **Phase 2.3 — Worker entry point.** Add `src/workers/simulation.worker.ts` as a thin adapter: it must contain the *only* code in this phase allowed to reference `self` or `postMessage`.
    1. **Separate the executable logic from the `self` binding.** Add `src/workers/executeRun.ts`, exporting `executeRun(request: RunRequestMessage, emit: (message) => void)`. It validates the dataset, constructs `createHistoricalBootstrapEngine(dataset, config.seed)`, calls `runSimulation` with `onBatchComplete` wired to `emit({ type: 'progress', runId, ... })`, and calls `emit` once more with a `ResultMessage` or `ErrorMessage`. Because `emit` is an injected plain function, this file has no Worker or DOM dependency and is unit-testable exactly like `core/`.
    2. **Keep `simulation.worker.ts` itself to a few lines.** `self.onmessage = (event) => executeRun(event.data, (message) => self.postMessage(message, isResultMessage(message) ? buildTransferList(message.result) : []))`. Only `ResultMessage`s carry a transfer list; `progress`/`error` messages are small enough to structured-clone normally.
    3. **Document the batch loop's complexity and memory layout in-place, per the Core Quant Finance & Monte Carlo Engine Standards.** Because this loop and its buffers are simulation-critical Web Worker code, annotate the batch-completion call site in `runSimulation.ts` with its time complexity relative to $N$ (paths) and $T$ (periods), and annotate `buildTransferList`/the flat `Float64Array` layout with why transferring ownership (versus cloning) avoids an $O(N \times T)$ copy and a matching garbage-collection pause on every run.
    4. **Prove `executeRun` end-to-end without a real Worker.** `executeRun.test.ts` drives it with the placeholder dataset and a small config, collecting every emitted message into a plain array, and asserts: at least one `progress` message with an increasing `pathsCompleted`, exactly one terminal message, a `result` message whose `SimulationResult` matches a direct `runSimulation` call with the same inputs, and an `error` message (not a thrown exception) when given a deliberately invalid config — mirroring Phase 1.5's "explicit failure, never a silent one" rule at the Worker boundary.
  - [x] **Phase 2.4 — React worker lifecycle hook.** Add `src/features/simulator/useSimulationWorker.ts`, the only place allowed to construct or terminate an actual `Worker` instance.
    1. **Model execution state as one `useReducer`, per the Maintenance Policy's "no global state library" rule.** States are exactly `idle`, `loading-data`, `running`, `completed`, `cancelled`, and `failed`, matching the Frontend Layout specification's Execution State section. Keep this state fully separate from whatever future input-configuration state Phase 4 adds — the Maintenance Policy requires input state and run state to never merge.
    2. **Mint a fresh run ID and a fresh `Worker` on every `run()` call.** Generate `runId` with `crypto.randomUUID()`, construct `new Worker(new URL('../../workers/simulation.worker.ts', import.meta.url), { type: 'module' })`, and `postMessage` one `RunRequestMessage`. A new `Worker` per run — not a reused long-lived one — is what makes `cancel()` in the next step a true hard stop rather than a cooperative flag the busy loop might not check for a while.
    3. **Implement `cancel()` as terminate-and-discard, exactly as the README specifies.** Call `worker.terminate()` on the current Worker, drop its reference, and transition to `cancelled`. Do not send a `cancel` message and wait for a cooperative response — `runSimulation`'s loop is synchronous and cannot check for one mid-batch.
    4. **Filter every incoming message through `isCurrentRun`.** `onmessage` first compares the message's `runId` against the hook's current `runId` and drops the message unmodified if they differ. This guards the real race the README calls out: the user edits an input and calls `run()` again while an old Worker's `progress`/`result` message is already in flight — the old message must never overwrite the new run's state.
    5. **Prove the reducer and the stale-message rule without a real Worker.** Because Vitest's jsdom environment does not implement `Worker`, test the reducer's transition table directly (`idle → running` on `run()`, `running → completed`/`failed` on a matching terminal message, `running → cancelled` on `cancel()`) and test that a hand-constructed message with a stale `runId` produces no state change, by calling the hook's internal message handler as a plain function. The actual `new Worker(...)` construction line stays deliberately thin and outside this unit-test boundary, matching how `simulation.worker.ts` itself stays thin around `executeRun`.
  - [x] **Phase 2.5 — uPlot quantile-band chart.** Add `src/charts/portfolioChart.ts` and a minimal wrapping component, rendering exactly the aggregate bands the Visualization spec requires — not yet the full retained-path click-through interactivity, which belongs to Phase 4. **Later amended** (see LOG.MD): the cross-sectional bands described in the numbered steps below were replaced with seven real representative paths (`selectRepresentativePaths` in `runSimulation.ts`), because a band's value at each period could come from a different underlying path than its neighboring periods and so never corresponded to an outcome any user actually experienced. The numbered steps below are kept as an accurate record of what this phase originally built, not of the chart's current behavior — see the Visualization and Metrics section above and the Core Engine Reference's `runSimulation.ts`/`quantiles.ts` entries for what replaced it.
    1. **Install `uplot` as the one new runtime dependency this phase adds**, matching the Technology Choices section. Do not add a heavier charting library or a React-specific chart wrapper package.
    2. **Map one `SimulationResult` to one chart-ready shape, in its own dependency-free module.** `src/charts/chartData.ts` exports `toChartData(result)`, returning the period-index axis plus the five quantile series, as one pure function with no DOM or `uplot` import — importing the `uplot` package itself touches browser globals like `matchMedia` as a module-load side effect, which breaks under Vitest's jsdom environment, so the pure mapping must live in a file the test never needs to pull `uplot` in to exercise. `src/charts/portfolioChart.ts` then imports both `uplot` and `toChartData` to do the actual rendering.
    3. **Render p10–p90 and p25–p75 as filled bands and p50 as a distinct line**, using uPlot's native `bands` option rather than hand-rolled canvas fill paths, so the chart stays inside one well-tested library feature instead of custom drawing code. Percentile bands must not be individually selectable, per the Visualization spec — this phase renders no per-path lines yet.
    4. **Keep the component a thin lifecycle wrapper.** Construct the `uPlot` instance on mount with `toChartData`'s output, call `.setData()` when a new completed result arrives, and call `.destroy()` on unmount — no simulation or Worker logic belongs in this file.
    5. **Prove `toChartData` alone.** `chartData.test.ts` checks the mapping against a small hand-built `SimulationResult`, including the period-index axis length and that every band array's values are non-decreasing across the same period sample the underlying `computeQuantileSeries` guarantees (p10 ≤ p25 ≤ p50 ≤ p75 ≤ p90). The uPlot instance itself is exercised by manual browser verification, not Vitest, matching the Maintenance Policy's "Playwright smoke suite for routing, running, cancellation, and one result render" — deferred to whenever that suite is added.
  - [x] **Phase 2.6 — Wire into the Phase 0 shell and close the phase.** Replace `SimulatorPage`'s Phase 0 raw-JSON dump with the real pipeline, keeping the surrounding UI intentionally minimal — the full Portfolio Construction / Simulation Inputs / Validation experience is Phase 4, not this phase.
    1. **Replace `placeholderRun.ts`'s call site, not the file's spirit.** `SimulatorPage` now calls `useSimulationWorker()`, renders **Run** (calling `run()` with the placeholder dataset and one fixed demo `SimulationConfig`) and **Cancel** (calling `cancel()`, disabled outside `running`), a short textual progress indicator while `running`, and the `PortfolioChart` once `completed`. Delete `placeholderRun.ts` and its test once nothing references them.
    2. **Surface `failed` and `cancelled` as visible, distinct states**, not as a silently empty chart — even this minimal shell must show *why* nothing is displayed, foreshadowing the fuller error-and-status handling Phase 4 will build out.
    3. **Run the complete completion gate.** The full Vitest suite (including every new Phase 2 test above), `npm run lint`, `npm run format`, and `npm run build` must all pass together. Manually verify in a running browser: **Run** shows progress and then a real chart built from the placeholder dataset, **Cancel** during a run actually stops it, and starting a second run while one is in flight never lets the first run's late messages corrupt the second run's displayed result.
    4. **Document the result in `LOG.MD`** with the same rigor as every Phase 1 entry: files added, operational behavior, the placeholder-dataset caveat repeated explicitly, and validation completed. Check off every Phase 2 sub-item here only once that entry exists.
- [x] **Dataset loader — real browser-side data, replacing the Phase 2 placeholder.** Added `src/core/data/loadDataset.ts` and a colocated test file, implementing the "Dataset Format and Loading" section above for exactly the one release the Dataset artifact gate has produced so far: weekly, USD (`usd-weekly-v1`). This subphase adds no new UI or Worker-side change — `executeRun`/`simulation.worker.ts`/`runSimulation` keep working against the exact same `AlignedDataset` shape they already validated in Phase 1. It does add one small, necessary extension to the existing execution state machine: because loading is now genuinely asynchronous, `useSimulationWorker`/`executionState.ts` gained a `load-failed` action so a fetch/checksum/slicing failure reaches the same `failed` state and error rendering a Worker-side failure already used, guarded by the same stale-run-id pattern already used everywhere else in that state machine.
  1. **Fetch the three release artifacts together, not just one.** Export a function such as `fetchDatasetArtifacts(frequency, baseCurrency)` that fetches `manifest.json`, `assets.json`, and the frequency/currency-specific `.f32` matrix the manifest names, and returns their parsed/raw forms together. Only `weekly`/`USD` exists today; any other combination must return a structured `ValidationResult` failure ("dataset not yet released for this frequency/currency") rather than a thrown fetch error or a silent fallback to the placeholder — an unreleased artifact is not a data-quality problem, and the "never silently clip invalid values into plausible results" rule applies to it just as much.
  2. **Parse `manifest.json` and `assets.json` with the browser's own strict JSON parser, and validate the manifest schema before trusting any field.** Use `response.json()` as-is; do not pre-process or tolerate non-standard tokens — this is exactly the class of bug the pipeline side just fixed for this file pair (see `LOG.MD`'s "Fixed invalid JSON in the released `assets.json`" entry). Validate `schemaVersion`, `datasetVersion`, `frequency`, `baseCurrency`, `dtype === 'float32'`, `byteOrder === 'little-endian'`, `layout === 'column-major'`, and that `columns`/`byteOffsets`/`assetColumns`/`specialColumns` are mutually consistent before using any of them — the runtime schema check the Dataset Format section already requires.
  3. **Fetch the matrix as an `ArrayBuffer` and verify its checksum and byte length before parsing a single value.** Compute a SHA-256 digest of the fetched bytes with `crypto.subtle.digest`, compare it against `manifest.checksum`, and separately verify `buffer.byteLength === manifest.rowCount * manifest.columns.length * 4`. Both checks must fail loudly — a structured error, never a console warning — before any column is sliced out, mirroring what `pipeline/validate_release.py` already does offline; a mismatch here means the deployed static file no longer matches the manifest describing it, a deployment/CDN problem the simulation must never silently run against.
  4. **Read one little-endian `Float32Array` view per requested column via `manifest.byteOffsets`, without copying the whole ~950 KB buffer per asset.** Each column occupies `manifest.rowCount * 4` contiguous bytes starting at `byteOffsets[columnName]`; construct one `Float32Array(buffer, byteOffset, rowCount)` view per needed column (the user's selected ≤6 `assetIds` plus `specialColumns.inflation` and `specialColumns.riskFreeRate`) instead of decoding every one of the 100 shipped columns — the same "typed arrays, no unnecessary copies" principle already applied to the Worker's transferable buffers in Phase 2.
  5. **Filter to the rows finite across exactly the selected columns — not the same operation as the pipeline's union-of-all-assets step, and not a single contiguous window either.** `AlignedDataset.assetReturns`/`inflation`/`riskFreeRates` must be entirely finite (`validateAlignedDataset` rejects any `NaN`), but the shipped matrix deliberately carries `NaN` before each asset's own inception (the Dataset artifact gate's union-not-intersection design) and can also carry an isolated internal gap in a series every selection depends on — confirmed directly against the real released `usd-weekly-v1` matrix, `CPI_INFLATION` is `NaN` for nine weeks in late 2025 because the raw FRED source has one blank monthly observation. Collect every row index where the selected asset columns, `CPI_INFLATION`, and `RISK_FREE_RATE` are simultaneously finite — in date order, not necessarily contiguous — and gather `dates` plus every selected `Float32Array` at exactly those indices. Rejecting the whole selection on any internal gap (rather than filtering the gap rows out) was tried first and found to be wrong: since `CPI_INFLATION` is required by every possible portfolio, that stricter rule would fail every selection whose history reaches into the gap, not just ones actually affected by it; the bootstrap engine samples one independent joint row per period with no notion of calendar adjacency (`historicalBootstrap.ts`), so dropping the non-finite rows and keeping every other jointly observed row is both correct and sufficient — dates stay strictly increasing either way. This is the loader's per-selection implementation of the Dataset Format section's "retain only rows that are finite for every selected asset and required special series" rule.
  6. **Build the `DatasetIdentity` and hand the result to the existing, unchanged `validateAlignedDataset`.** Populate `identity.version` from `manifest.datasetVersion` (never a placeholder string), `identity.checksum` from the already-verified `manifest.checksum`, and `identity.frequency`/`identity.baseCurrency` from the manifest. Run the resulting `AlignedDataset` through Phase 1.1's `validateAlignedDataset` before returning it — the loader must not reimplement the 260-week minimum, duplicate-asset, or finite-value checks that module already owns; a selection with too little common history after step 5's trim is exactly the case that check already catches.
  7. **Cache the fetched artifacts in memory for the session, per the Dataset Format section, fetching each frequency/currency combination at most once.** A second asset selection within the same session must reslice the already-downloaded manifest/matrix, not refetch them; only a change of frequency or base currency (not user-selectable until Phase 4's real inputs exist) should trigger a new `fetch`. Keep this cache local to the loader module — it is not simulation or execution state, so it does not belong in `useSimulationWorker`'s reducer.
  8. **Replace `createPlaceholderDataset()` at exactly one call site.** `SimulatorPage.tsx`'s `handleRun` calls the new loader instead, using a fixed demo asset selection (for example two real tickers already in `manifest.assetColumns`, such as `SPY` and `AGG`) in place of the Phase 2 synthetic two-asset placeholder, and updates the on-page `phase-note` text accordingly. `DatasetIdentity.version` no longer contains the literal string `placeholder` once this lands, so a completed result can no longer be mistaken for synthetic Phase 2 output. Loading the artifacts is asynchronous, so `handleRun` must dispatch the existing `loading-data` execution state before `run()` is called with the resolved dataset, and a fetch/parse/checksum failure must reach the same `failed` state and error rendering the Worker path already provides — not a second, parallel error UI.
  9. **Prove every rule with fixtures, never a real network fetch.** Build a small in-memory manifest object plus a hand-built `ArrayBuffer` (a handful of rows, three or four columns) directly in the test file, covering: a valid fetch-and-slice round trip against a hand-checked expected `AlignedDataset`; a checksum mismatch; a byte-length mismatch; a requested frequency/currency combination absent from the manifest; a selected asset ID absent from `assetColumns`; a selection whose common finite row count falls below the 260-week minimum; and an isolated internal gap in one required column being filtered out of the result rather than rejecting the whole selection. Stub `fetch`/`crypto.subtle.digest` directly in the test rather than hitting the real `public/data/` files, so this suite stays fast and deterministic exactly like every other `core/` test — then cross-check the real implementation against the actual released `public/data/` files at least once outside Vitest (e.g. a throwaway script against a local static server), since a hand-built fixture cannot by construction reproduce a real, previously-unknown gap like the `CPI_INFLATION` one this step already found.
  10. **Completion gate.** Full Vitest suite, `npm run lint`, `npm run format`, and `npm run build` passing together; manual browser verification that **Run** shows "Preparing dataset…", fetches the real ~950 KB matrix exactly once, and produces a chart built from real historical `SPY`/`AGG` weekly returns rather than the synthetic placeholder. Document files added, the exact demo asset selection used, and validation results in `LOG.MD` before checking this phase off here.
- [x] **Phase 3 — Parametric Student's $t$ engine.** Add the second live engine behind the exact `SimulationEngine` boundary defined in Phase 1.1 and exploited unchanged since: fit a multivariate Student's $t$ model to the aligned dataset's periodic log returns, then sample joint scenarios from it. `runSimulation`, `stepPortfolioPeriod`, the Worker protocol's progress/cancel/transfer machinery, and the chart all remain byte-for-byte unchanged except where 3.5 explicitly says otherwise — that reuse is the payoff this phase exists to collect. Everything in 3.1–3.4 is headless `core/` code with colocated tests; 3.5 is the only step allowed to touch `src/workers/` or `src/features/`; 3.6 is the proof gate, mirroring Phase 1.6.
  - [x] **Phase 3.1 — Estimation statistics.** Add `core/math/statistics.ts` with the pure estimation functions the fit step needs, each one hand-testable against a spreadsheet or NumPy in isolation.
    1. **Convert simple returns to log returns.** `toLogReturns(simpleReturns)` maps $\ell_t=\ln(1+r_t)$ element-wise, reading the dataset's `Float32Array` column and returning a `Float64Array`. *Intuition:* log returns add across periods and are unbounded below/above, which is what makes a symmetric elliptical model (Student's $t$) a defensible shape for them — a simple return is floored at $-100\%$ and cannot be symmetric. The engine simulates $\ell$ and converts back at the very end (3.4), so accounting still sees simple returns.
    2. **Sample mean and covariance with the standard unbiased estimators.** `sampleMean(values)` and `sampleCovarianceMatrix(columns)`, the latter returning a $D\times D$ symmetric matrix using the $n-1$ divisor. Matrices throughout Phase 3 are plain `number[][]` — at $D\le6$ a $36$-element structure, where readability beats any typed-array layout gain; the per-period hot loop (3.4) touches only the precomputed Cholesky factor, never re-estimates.
    3. **Pool standardized residuals and estimate excess kurtosis.** `computePooledExcessKurtosis(columns)`: standardize each asset's log-return column by its own sample mean and sample standard deviation, pool all $n\times D$ standardized values into one sample, and return $\gamma_2=m_4/m_2^2-3$ using plain moment ratios of the pooled sample. *Intuition:* one shared $\nu$ governs tail thickness for the whole joint distribution, so the evidence for it is pooled across assets rather than estimated per-asset and averaged; standardizing first stops a high-volatility asset from dominating the pooled fourth moment.
    4. **Estimate degrees of freedom by moment matching.** `estimateDegreesOfFreedom(excessKurtosis)` implements the README's rule verbatim: $\nu=4+6/\gamma_2$, clamped to $[5,100]$, with $\nu=100$ whenever $\gamma_2\le0$. *Intuition:* a Student's $t$ with $\nu$ degrees of freedom has excess kurtosis $6/(\nu-4)$ for $\nu>4$; inverting that maps measured tail weight to $\nu$. The clamp floor keeps the fourth moment finite ($\nu>4$) with margin; the ceiling acknowledges that beyond $\nu\approx100$ the $t$ is indistinguishable from a Gaussian in double precision.
    5. **Prove with hand-checked and NumPy-checked values.** Tests cover: a log-return conversion checked by hand; mean/covariance of a tiny fixture against hand arithmetic; pooled kurtosis of a known sample against an independently computed value; the $\nu$ formula at exact boundary inputs ($\gamma_2=0$, negative, the value mapping exactly to $\nu=5$, and one interior value).
  - [x] **Phase 3.2 — Matrix routines: nearest correlation and Cholesky.** Add `core/math/matrix.ts`. Everything here operates on small dense symmetric matrices ($D\le6$), so clarity wins every layout decision, but numerical failure modes still must be explicit, per the Financial Rules' "never silently clip invalid values" — a covariance estimated from nearly collinear return series can genuinely fail these routines.
    1. **Convert between covariance and correlation.** `covarianceToCorrelation(covariance)` returns the correlation matrix plus the standard-deviation vector; `correlationToCovariance(correlation, standardDeviations)` reassembles. This pair is what makes "repair the correlation while preserving asset variances" implementable: repair happens in correlation space, variances re-enter untouched afterward.
    2. **Symmetric eigendecomposition via the cyclic Jacobi method.** `jacobiEigenDecomposition(matrix)` — repeated $2\times2$ rotations zeroing the largest off-diagonal entry until all are below a documented tolerance. *Why Jacobi and not a library:* the Maintenance Policy forbids adding packages for short deterministic algorithms; at $D\le6$ Jacobi converges in a handful of sweeps, is simple enough to review line-by-line, and its accuracy for symmetric matrices is excellent. Complexity $\mathcal{O}(D^3)$ per sweep — irrelevant at $D\le6$, annotated anyway per the engine standards.
    3. **Project onto the positive-semidefinite cone.** `projectToPositiveSemidefinite(matrix)`: eigendecompose, floor negative eigenvalues at zero, reconstruct $Q\Lambda_+Q^\top$. This is the $P_S$ step of Higham's algorithm and the canonical answer to "what is the nearest valid covariance-like matrix to this slightly-broken one."
    4. **Higham's alternating-projections nearest-correlation algorithm.** `nearestCorrelationMatrix(matrix)`: alternate $P_S$ (PSD projection) and $P_U$ (restore unit diagonal) with Dykstra's correction between iterations, until the iterate stops changing within a documented tolerance or a documented iteration cap is hit (a structured failure, not an infinite loop). *Intuition:* an estimated correlation matrix can be indefinite (near-collinear series, missing-data patterns); sampling from it would mean sampling a "distribution" with negative variance directions. Higham's method finds the provably nearest true correlation matrix in Frobenius norm. Dykstra's correction is what makes the alternation converge to the nearest point of the *intersection* rather than drifting.
    5. **Cholesky with an explicit tolerance.** `choleskyDecompose(matrix)` returns a `ValidationResult` of the lower-triangular factor $L$ with $LL^\top=$ input; a non-positive pivot beyond a documented relative tolerance returns a structured `matrix.cholesky` error rather than `NaN`s. *Intuition:* $L$ is the "square root" of the covariance that turns $D$ independent unit-variance draws into one correlated draw; it is computed once at fit time, then reused for every one of the up to $10{,}000{,}000$ scenario draws.
    6. **Prove against known matrices.** Tests: a hand-checked $2\times2$ and $3\times3$ Cholesky; eigendecomposition of a matrix with known eigenvalues; PSD projection of a deliberately indefinite matrix (checked against an independently computed result); Higham repair of a known non-PSD "correlation" matrix, asserting unit diagonal, symmetry, PSD-ness, and closeness to the independently computed repaired matrix; and a Cholesky failure case returning a structured error.
  - [x] **Phase 3.3 — Distribution sampling primitives.** Add `core/math/distributions.ts`: draws layered on the existing `RandomGenerator`, never on `Math.random()`. Every algorithm choice here is part of a run's reproducible identity — the exact uniform-consumption order is fixed by these functions, so changing any of them later requires a model-version bump in 3.4.
    1. **Standard normal via Box–Muller, one draw per call, no cached spare.** `nextStandardNormal(random)` draws $u_1$ from $(0,1]$ as $1-\text{nextUniform()}$ (so $\ln u_1$ is always finite), $u_2$ from $[0,1)$, and returns $\sqrt{-2\ln u_1}\cos(2\pi u_2)$ — deliberately discarding the sine companion. *Trade-off, documented in-place:* caching the spare normal would halve uniform consumption but make the generator stateful, so the number of uniforms consumed by a scenario would depend on parity of the draw history — a subtle reproducibility hazard for an independent reimplementation. Two uniforms per normal at $\le6.3$ million normals per maximum-work run is microseconds of extra arithmetic; determinism that is trivial to describe in prose wins.
    2. **Gamma via Marsaglia–Tsang, valid for shape $\ge1$.** `nextGamma(random, shape)`: $d=\text{shape}-\tfrac13$, $c=1/\sqrt{9d}$; repeat { $x=$ normal, $v=(1+cx)^3$; reject $v\le0$; $u=$ uniform; accept when $\ln u<\tfrac{x^2}2+d-dv+d\ln v$ } — using the log acceptance test unconditionally rather than the common squeeze fast-path, so the uniform-consumption sequence is describable in one sentence. Shape $<1$ throws: 3.4's clamp $\nu\ge5$ guarantees shape $=\nu/2\ge2.5$, so the boost transform is dead code this phase refuses to ship untested.
    3. **Chi-square as a gamma special case.** `nextChiSquare(random, degreesOfFreedom)` returns $2\cdot\text{nextGamma}(\text{random},\nu/2)$ — the one consumer this phase needs, for the $t$ mixing variable.
    4. **Prove distributional shape and exact determinism.** Tests: fixed-seed reference sequences (exact values, guarding the draw-order contract); same-seed reproducibility and different-seed divergence; and statistical checks on moderately sized fixed-seed samples — normal sample mean/variance near $0/1$, gamma sample mean near shape, chi-square sample mean near $\nu$ — with predeclared tolerances, as regression guards rather than proofs of asymptotic correctness.
  - [x] **Phase 3.4 — Fit and engine factory.** Add `core/simulation/parametricStudentT.ts`, exporting `PARAMETRIC_STUDENT_T_MODEL_VERSION = 'parametric-student-t-v1'`, `fitParametricStudentT(dataset, options)`, and `createParametricStudentTEngine(model, seed)`.
    1. **Separate fitting from sampling.** `fitParametricStudentT` is a pure function from an already-validated `AlignedDataset` plus user options to a `ValidationResult` of a frozen `ParametricStudentTModel` — per-period log-return location vector, Cholesky factor of the repaired scale matrix, $\nu$, per-period inflation and risk-free constants, and fit diagnostics (measured $\gamma_2$, whether Higham repair actually changed the matrix). The factory then only samples. *Consequence:* every numerical failure (bad option, Cholesky failure) is a structured `ValidationResult` error surfaced before any Worker loop starts, and the fit is independently testable without drawing a single sample.
    2. **Location vector: historical by default, user-entered annual geometric returns as override.** Historical: $\mu_i=$ sample mean of asset $i$'s log returns — the maximum-likelihood-flavored drift whose annualization *is* the historical geometric return. Override: a user-entered annual geometric return $g_i$ becomes $\mu_i=\ln(1+g_i)/p$ with $p$ periods per year (52 weekly, 12 monthly). Covariance always comes from the aligned data (the frontend spec explicitly does not expose an editable matrix), so an override moves the distribution without reshaping it.
    3. **Scale matrix and repair, in the README's stated order.** $\Sigma$ = sample covariance of log returns; $S=\Sigma(\nu-2)/\nu$ (a $t_\nu$ with scale $S$ has covariance $S\cdot\nu/(\nu-2)$, so this makes the simulated covariance match the measured $\Sigma$); split $S$ into correlation and standard deviations, run `nearestCorrelationMatrix` on the correlation (recording whether it changed anything), reassemble with the untouched standard deviations, then `choleskyDecompose`. Degrees of freedom: automatic via 3.1's estimator, or a manual override validated to the same $[5,100]$ range.
    4. **Constant per-period inflation and risk-free rate, converted from user-entered annual values.** Per the Financial Rules, parametric mode uses user-entered constants, not sampled history: the annual risk-free rate de-annualizes geometrically to $(1+r_f)^{1/p}-1$ (an effective periodic simple rate, matching the dataset column's units), and annual inflation to the per-period *log* increment $\ln(1+\pi)/p$ (matching the dataset's CPI column, which ships log increments). Both conversions are documented at the definition site because the two columns' units genuinely differ.
    5. **The sampling rule, and its draw order as contract.** Each `nextScenario()`: draw $z_1,\dots,z_D$ standard normals in asset order, then one $\chi^2_\nu$; the joint log-return vector is $\ell=\mu+Lz\sqrt{\nu/\chi^2_\nu}$, returned as simple returns $r_i=e^{\ell_i}-1$. *Intuition:* $Lz$ builds the correlated Gaussian body; the shared $\sqrt{\nu/\chi^2_\nu}$ multiplier stretches all assets *together* in a fat-tailed period, which is exactly the joint-crash behavior independent per-asset $t$ draws would miss. $e^\ell-1>-1$ always, so the dataset's $-100\%$ floor holds by construction. `sourceRowIndex` is `null` — a sampled scenario has no historical row; the `PeriodScenario` contract widens to `number | null` and the bootstrap engine keeps returning real indices.
    6. **Prove the fit and the engine separately.** Fit tests: a hand-checkable two-asset fixture asserting location, $S$, $\nu$, and the option overrides; a Cholesky/repair failure surfacing as a structured error; option-validation errors. Engine tests: fixed-seed exact scenario values (the reference trace 3.6 will independently reproduce); same-seed reproducibility, different-seed divergence; fresh arrays per call; and statistical checks on a fixed-seed sample — the generated sample correlation near the fitted target, sample mean near $\mu$, and measured excess kurtosis of generated standardized returns positive and larger for small $\nu$ than large $\nu$ (the fat-tail claim, measured rather than asserted).
  - [x] **Phase 3.5 — Worker and shell wiring: engine selection as a discriminated union.** The only step touching `src/workers/` and `src/features/`. Per the coding standards' "type-safe variant selection," the Worker request now carries *which* engine to build, not a prebuilt engine (an engine closure cannot cross a structured-clone boundary).
    1. **Extend the message protocol.** `EngineSelection = { engine: 'bootstrap' } | { engine: 'studentT', options: ParametricStudentTOptions }` replaces the `modelVersion`/`prngVersion` fields of `RunRequestMessage` — the Worker side is the one place that knows which engine it constructs, so it now supplies the version strings itself instead of trusting the caller to keep two loosely-coupled arguments consistent (closing the "documented convention, not compiler-enforced" gap Phase 1.5 flagged).
    2. **`executeRun` builds the selected engine.** An exhaustive `switch` on `selection.engine`: bootstrap constructs exactly as today; studentT runs `fitParametricStudentT` (a `ValidationResult` failure becomes the existing `ErrorMessage` path) then `createParametricStudentTEngine`. The `runSimulation` call, batching, progress, and transfer list are untouched.
    3. **Minimal engine selector on the shell.** `SimulatorPage` gets the frontend spec's three-button selector in minimal form: **Bootstrap** and **Parametric** enabled, **Markov Chain** rendered disabled and labelled "future". Parametric runs with fixed demo options (historical means, automatic $\nu$, 2% annual inflation, 3% annual risk-free) — real editable inputs are Phase 4's job, unchanged.
    4. **Prove at the same boundaries as Phase 2.** `executeRun.test.ts` gains: a studentT run emitting progress and one result whose metadata carries the parametric model version; a fit failure emitting one `ErrorMessage`; and an assertion that bootstrap results are byte-identical to before the protocol change (same seed, same placeholder dataset). Existing hook/reducer tests keep passing unmodified — the state machine never sees the engine choice.
  - [x] **Phase 3.6 — Proof and completion gate.** No new production code, mirroring Phase 1.6.
    1. **Independent Python reimplementation.** `validation/phase_3_independent_check.py`: from the prose above (not the TypeScript source), reimplement Box–Muller, Marsaglia–Tsang, the estimation pipeline (log returns, mean, covariance, pooled kurtosis, $\nu$, scale conversion, Higham repair via NumPy's own eigendecomposition, Cholesky), and the scenario rule; print a fixed-seed, few-period reference trace and the fitted-model numbers for the same Float32-rounded fixture the TypeScript tests use.
    2. **Cross-check test.** `src/core/simulation/studentTIndependentCheck.test.ts` asserts the TypeScript fit (location, $\nu$, Cholesky factor entries) and a fixed-seed scenario sequence against the Python trace, period by period — the same "two independent implementations must agree" logic as Phase 1.6, now covering the transcendental-function chain (`exp`/`log`/`cos`/`sqrt`) that Phase 1's integer-and-arithmetic-only check never exercised.
    3. **Full gate.** Complete Vitest suite, `npm run lint`, `npm run format`, `npm run build` all passing together; document files, behavior, the fitted demo-portfolio numbers, and validation results in `LOG.MD`; check off every Phase 3 item here only once that entry exists.
  - [x] **Phase 4 — Real input/configuration UI.** Replace the minimal shell with the full Portfolio Construction, Simulation and Portfolio Inputs, Execution State, Results, and Validation/Accessibility behavior defined in "Frontend Layout and Interaction Specification" — but **only the controls whose behavior the engines and accounting already implement**. Rebalancing, tax, transaction-cost, and leverage controls arrive in Phase 5+ together with their accounting: a rendered control that silently does nothing is the UI form of the silent clipping the Financial Rules forbid. The `useSimulationWorker`/`executionState.ts` execution machine remains separate from the headless input-configuration reducer.
  - [x] **Phase 4.1 — Browser verification gate (no new code).** Pay down the disclosed debt from the Dataset loader, chart redesign, and Phase 3 entries before building more UI on top of it: a real-browser click-through of the current shell against the released `usd-weekly-v1` artifacts.
    1. **Both engines end to end.** Run → progress → rendered chart for Bootstrap and Parametric; sanity-check the on-screen representative terminal values against the numbers already logged offline for the same 60/40 SPY/AGG demo (bootstrap 58,847…220,554; parametric 58,327…223,984).
    2. **The state machine's hard paths.** Cancellation mid-run; starting a second run while one is in flight (stale run-id messages must be ignored); a dataset failure surfacing through the `load-failed` action; direct reloads of `/`, `/education`, `/resources`.
    3. **Record it.** Document what was seen in `LOG.MD`. Any defect found becomes a fix plus a regression test before 4.2 begins.
  - [x] **Phase 4.2 — Headless per-path metrics.** Add `core/portfolio/metrics.ts`, exporting `METRICS_VERSION = 'metrics-v1'`, and extend `runSimulation` additively (the Phase 2.2 pattern) so metrics accumulate **streaming inside the existing path loop**, where each period's `contribution`, `neutralReturn`, and scenario `riskFreeRate` are already in hand — never by re-walking the flat equity buffer afterwards. Time $\mathcal{O}(N \times T)$, space $\mathcal{O}(N)$ beyond what the runner already holds.
    1. **One pure function per metric, per path.** CAGR (lump-sum only): $(W_T/C_0)^{1/\text{years}}-1$. Annualized volatility: sample standard deviation (ddof = 1) of the path's contribution-neutral periodic returns, scaled by $\sqrt{52}$ or $\sqrt{12}$ — `neutralReturn` exists precisely so a DCA top-up never masquerades as investment performance. Sharpe: arithmetic excess of `neutralReturn` over the same period's sampled risk-free rate, mean over standard deviation, annualized by $\sqrt{p}$. Maximum drawdown: on the unitized index $U_t=\prod_s(1+\text{neutralReturn}_s)$ with streaming peak tracking, so contributions cannot fill in a drawdown. IRR (DCA/value averaging): the periodic rate solving $\mathrm{NPV}=0$ for the path's actual dated external cash flows (initial investment, each period's realized contribution, terminal value), found by deterministic bracketed bisection — no `Math.random`-style nondeterminism, no Newton divergence — annualized as $(1+r)^p-1$; `null` when no finite admissible root exists.
    2. **Report distributions, not one number.** Every path is one draw, so each metric is computed per path and summarized across paths with the canonical `computeQuantile` (p10/p50/p90); the table's headline value is the median, labelled as such. Loss probability is the fraction of paths with $W_T < C_0+\sum_t C_t$ using **that path's own** realized contributions (value-averaging contributions differ per path). Ruin probability is the fraction of failed/insolvent paths — structurally 0 until leverage exists in Phase 5+; report it as 0 with that reason rather than omitting it.
    3. **Contract and tests.** `SimulationResult` gains a `metrics` block and `metadata.algorithms` gains `METRICS_VERSION`, per the Financial Rules' versioning requirement. Tests: hand-calculated two-to-three-period paths per metric; degenerate cases (zero-volatility path → Sharpe `null`, wiped-out path, no-admissible-IRR-root path); and the invariant that a lump-sum path's annualized IRR equals its CAGR.
  - [x] **Phase 4.3 — Asset catalogue loading.** Add `core/data/assetCatalogue.ts` plus a colocated test file: the runtime contract for `assets.json`, which is untrusted JSON and must be schema-validated at runtime per the Maintenance Policy, exactly as `manifest.json` already is in `loadDataset.ts`.
    1. **Validate, then expose.** `validateAssetsCatalogue` checks schema version, dataset-version agreement with the manifest, unique non-empty `assetId`s, and the presence/types of the fields the UI consumes (`ticker`, `name`, `assetClass`, `history.firstDate/lastDate/rowCount/meetsWeeklyMinimum`), returning the shared `ValidationResult` shape. `loadAssetsCatalogue` fetches and validates once per session, cached in memory like the return matrix.
    2. **Honest history preview.** From the selected holdings' `history` spans, derive an **estimated** common-history summary (latest `firstDate`, earliest `lastDate`, estimated overlap count) for the picker's read-only display. Document in code that this is advisory: interior gaps mean the authoritative common-row count is only known after `loadAlignedDataset`'s finite-row alignment, whose existing 260-week minimum check remains the binding gate and fails the run through the established error path.
  - [x] **Phase 4.4 — Input-configuration state.** Add `src/features/simulator/simulatorState.ts` (the file the Recommended Project Structure reserved for exactly this): a plain, headless reducer for everything the user edits, tested without rendering. Execution state is untouched.
    1. **The state shape.** Holdings (at most six rows of `assetId` + allocation percentage), engine choice with parametric options (per-holding return mode `historical` or a manual annual geometric return, $\nu$ mode automatic or manual within $[5,100]$, annual inflation, annual risk-free rate), seed, path count, horizon years, frequency (weekly; monthly is represented but disabled until a monthly artifact exists), initial investment, and the cash-flow discriminated union. Defaults per the frontend spec: weekly, 2,000 paths, 10 years.
    2. **Derived, not stored.** `toSimulationConfig(state)` produces the exact `SimulationConfig` and `EngineSelection` the Worker protocol already accepts — the demo constants in `SimulatorPage` disappear rather than gaining edit fields. `validateInputs(state, catalogue)` layers the UI-level checks (allocation total 100% within 0.01 pp, work budget $\text{paths}\times\text{periods}\le10{,}000{,}000$ including the spec's derived selectable-path maximum, six-holding limit, estimated history minimum) on top of the existing core validators, returning the same structured `ValidationError` list so every error can render beside its control.
  - [x] **Phase 4.5 — Input workspace components.** The React layer over 4.3 + 4.4: the desktop two-column workspace (portfolio construction left; simulation and portfolio settings right), stacked in the spec's order on narrow screens.
    1. **Portfolio Construction.** A keyboard-searchable ETF picker over the validated catalogue (combobox pattern: filter by ticker and name, full keyboard operation), holding rows showing ticker, name, allocation input, and remove control, and a continuously displayed allocation total.
    2. **Simulation Inputs.** The existing three-button engine selector, seed, paths, horizon, and frequency (monthly rendered disabled with an on-control reason until its artifact gate). Bootstrap shows the read-only common-history summary from 4.3. Parametric shows per-holding return overrides (defaulting to the displayed historical annualized geometric value), automatic/manual $\nu$, annual inflation, and annual risk-free rate — the four demo constants Phase 3 hardcoded.
    3. **Portfolio settings.** Initial investment and the cash-flow mode selector with its mode-specific field (DCA amount, value-averaging target increase). Nothing else: no tax, cost, rebalancing, or leverage controls yet.
    4. **Validation and accessibility, as specified.** Errors beside their controls plus an `aria-live` summary region; Run disabled while inputs or data are invalid; no automatic rerun on field changes; semantic `fieldset`/`legend`/labels, visible focus, and status cues not relying on color alone. Testing Library tests cover the search→select→weight→run flow and each validation message.
  - [x] **Phase 4.6 — Results: metrics table, previous-run labelling, path inspection.** The consumption side of 4.2, completing the spec's Results section for the implemented feature set.
    1. **Metrics table.** Terminal-wealth percentiles, loss probability, ruin probability (with its "no leverage yet" reason), CAGR or IRR per cash-flow mode, annualized volatility, maximum drawdown, and Sharpe ratio, each rendering `N/A` with a short reason when undefined and linking its definition to the Educational route.
    2. **Previous-run labelling.** Keep the last completed result visible while inputs change — already free, since execution state survives input edits — but label it with the run's seed, engine, and settings summary so an edited form can never be misread as describing the chart below it.
    3. **Retained-path inspection.** Click a retained or representative path to inspect its period-by-period equity, contributions, and (bootstrap only) each period's `sourceRowIndex` mapped back to its historical date — the honesty payoff of retaining real paths. Provide the tabular alternative for essential chart values required by the accessibility spec.
    4. **Nominal/real display toggle.** Deflate displayed path values by each path's own jointly sampled inflation trajectory (parametric mode: the constant annual assumption), which requires one additive runner extension: retain a cumulative price-level series for retained/representative paths only — never for all $N$ paths, keeping transfer size bounded. Accounting stays nominal per the Financial Rules; this is display-layer division only.
  - [x] **Phase 4.7 — Proof and completion gate.** No new production code. Add the Maintenance Policy's small Playwright smoke suite — routing, one full run, cancellation, one result render — now that a stable real UI exists to point it at. Complete Vitest suite, `npm run lint`, `npm run format`, and `npm run build` all passing together; a real-browser pass over the full new workspace (both engines, at least one three-plus-asset portfolio, one invalid-input walk); document everything in `LOG.MD` and check off Phase 4 here only once that entry exists.
- [x] **Phase 5 — Benchmark comparison.** First item of Implementation Order step 6. Add one optional ETF benchmark as a separate $1\times$ buy-and-hold account. Its purpose is to answer a narrow counterfactual: “given the exact cash the investor actually contributed to this portfolio path, what would buying and holding this one ETF have produced?” It is not a second allocation, does not consume one of the six portfolio-holding slots, and must never feed into portfolio weights, value-averaging targets, rebalancing, costs, taxes, debt, or margin logic. This phase reports terminal comparison only; it deliberately does not add an $N\times T$ benchmark-path matrix merely to draw a misleading single benchmark line when value-averaging makes benchmark cash flows path-specific. A future chart extension may retain or recompute explicitly selected benchmark trajectories with a documented memory policy.
  - [ ] **Phase 5.1 — Model the benchmark selection without contaminating portfolio accounting.** Add an optional `benchmarkAssetId: string | null` to the headless input state, a reducer action to set/clear it, and a separate benchmark picker in `PortfolioConstruction.tsx`. It searches the same validated catalogue but is outside the holding-table count, allocation total, and weight validation. Selecting an asset already held is valid: it represents comparing the allocation against buy-and-hold in that same asset, and the loader must not request its matrix column twice.
    1. **Make the loaded dataset a joint-universe dataset.** `deriveRunPlan` returns the ordered unique union of portfolio holding IDs followed by the benchmark ID when it is not already present. `SimulatorPage` passes that union to `loadAlignedDataset`, so the existing finite-row alignment applies to every return series the comparison needs. Both the bootstrap engine and Student's $t$ fit/sample the full union jointly; the benchmark therefore shares the same sampled historical row or correlated parametric shock as the portfolio. Do not build a second independently seeded engine for the benchmark: that would turn a paired comparison into additional sampling noise.
    2. **Add an explicit projection contract at the runner/Worker boundary.** `RunPlan`, `RunSimulationRequest`, `RunRequestMessage`, and `RunSimulationInput` gain a small immutable selection object: ordered `portfolioAssetIndices` and `benchmarkAssetIndex: number | null`, indexing the loaded `AlignedDataset.assetIds`. Validate that portfolio indices are non-empty, unique, in bounds, and have the same count as `config.weights`; validate that a benchmark index is in bounds. The benchmark index may equal a portfolio index, but must never appear as an extra portfolio weight. `runSimulation` validates `SimulationConfig` against `portfolioAssetIndices.length`, projects only those scenario returns into `stepPortfolioPeriod`, and reads the optional benchmark return from its own dataset index. This removes the current implicit assumption that every dataset column is a portfolio holding while keeping `SimulationConfig` focused on portfolio accounting.
    3. **Preserve no-benchmark compatibility.** The selection contract's no-benchmark form uses the identity portfolio index sequence and `benchmarkAssetIndex: null`. Existing direct `runSimulation` tests and Worker fixtures should be migrated through that explicit identity form; their bootstrap terminal values and PRNG draw order must remain byte-for-byte unchanged. A malformed selection returns structured validation errors before the path loop, never an out-of-bounds `undefined` return reaching accounting.
  - [ ] **Phase 5.2 — Implement one pure benchmark accumulator.** Add `core/portfolio/benchmark.ts` plus colocated tests. Export a minimal immutable state/result function, for example `stepBenchmarkPeriod(previousValue, benchmarkReturn, contribution)`, and an initializer from `initialInvestment`.
    1. **Use the same timing as portfolio cash flows.** For every period compute $B_t^- = B_{t-1}(1+r^{\mathrm{benchmark}}_t)$, then $B_t = B_t^- + C_t$. The runner supplies `C_t` from the already-computed portfolio `stepPortfolioPeriod` result; it does not ask the benchmark to calculate DCA or value averaging. Initial investment enters at period zero, and each later contribution arrives after that period’s benchmark return, exactly matching the portfolio’s end-of-period contribution rule.
    2. **Keep Phase 5 deliberately frictionless and isolated.** There is one scalar benchmark value per path, no target weights, no trade list, no rebalancing, no transaction costs, no cost basis, no taxes, no debt, and no leverage. A non-finite benchmark return or value is an explicit path failure with its own stable failure code; never substitute zero or silently drop that comparison. Portfolio failures and benchmark failures are tracked separately so a future engine defect cannot be reported as investment underperformance.
    3. **Prove the accumulator with hand arithmetic.** Cover initial allocation, return-before-contribution timing, a two-period DCA example, and a value-averaging example where the portfolio’s realized second contribution differs from its first. The key invariant is cash-flow identity: for every period the benchmark receives exactly the `contribution` emitted by the portfolio accounting step, including zero contributions when a value-averaging target is already exceeded. Also prove that a benchmark’s own return never changes that contribution.
  - [ ] **Phase 5.3 — Extend runner results and comparison metrics.** Keep one benchmark terminal-value `Float64Array` alongside `terminalWealth`, plus one scalar accumulator per active path: additional runner space is $\mathcal{O}(N)$, not another full $\mathcal{O}(N\times T)$ history. Extend `SimulationResult` with benchmark terminal outcomes only when a benchmark was selected, and add a versioned `metrics.benchmark` block carrying the benchmark asset ID, paired terminal-value-difference summary, outperformance probability, and comparable-path count.
    1. **Define paired comparison statistics precisely.** For every path where both terminal values are finite, calculate $\Delta W_T = W_T^{\mathrm{portfolio}} - W_T^{\mathrm{benchmark}}$. Summarize $\Delta W_T$ across comparable paths with the canonical linear-interpolation p10/p50/p90 rule. Outperformance probability is $\#\{\Delta W_T>0\}/\#\{\text{comparable paths}\}$; an exact tie is not an outperforming path. Failed or non-finite pairs are excluded from this paired denominator and surfaced through the existing failure reporting rather than counted as an arbitrary loss or win.
    2. **Keep result transport and exports forward-compatible.** Transfer the benchmark terminal typed array in `buildTransferList` only when present. Keep the full all-path terminal array in the result so Phase 9 can export paired outcomes without rerunning the simulation. Do not retain per-period benchmark arrays in retained or representative paths in this phase; their absence is an intentional memory contract, not a missing clone.
    3. **Prove the runner pairing end to end.** Use a tiny scripted engine and a two-asset joint dataset where portfolio and benchmark returns are distinct. Assert the benchmark gets the portfolio’s actual value-averaging contributions, not contributions calculated from benchmark value; assert one exact terminal difference per path; assert ties, portfolio wins, and benchmark wins; assert invalid projection/benchmark indexes fail structurally; and assert no-benchmark runs retain all pre-Phase-5 result values and transfer-list behavior.
  - [ ] **Phase 5.4 — Present and validate the comparison.** Add the separate optional benchmark control and a clear/remove action to the portfolio workspace. `ResultsPanel` renders no comparison section when unset; when set, it names the ETF, shows paired terminal-value difference p10/median/p90, the percentage of comparable paths that outperformed, and a short disclosure that the benchmark received the portfolio’s realized contributions on matching simulated dates. It must not describe this as an independently optimized alternative or as a benchmark line.
    1. **Accessibility and state behavior.** Give the selector a visible label, keyboard-operable search results, and field-addressable validation errors. Editing/clearing the benchmark while a run is active follows the existing input-change cancellation rule. Completed results retain their own benchmark identity in result metadata, so changing the next-run picker cannot relabel a prior comparison.
    2. **Completion gate.** Add focused tests for the benchmark accumulator, input derivation, runner/Worker projection and transfer list, and result rendering. Run the complete Vitest suite, `npm run lint`, `npm run format`, `npm run test:e2e`, and `npm run build`; manually run Bootstrap and Parametric comparisons with DCA and value averaging. Record the benchmark contract, paired denominator, memory decision, and validation results in `LOG.MD` before checking this phase off.
- [x] **Phase 6 — Rebalancing.** Add frictionless portfolio rebalancing after Phase 5, before transaction costs and taxes. Rebalancing changes the composition of the same portfolio equity; it never creates an external contribution, changes the already-computed benchmark cash flow, or changes neutral return for the completed period. The output is an explicit canonical per-asset trade list in signed base-currency value, not just replacement holdings, because Phase 7 must price those exact buys and sells, adjust cost basis, and tax realized gains without reverse-engineering intent from post-trade balances.
  - [ ] **Phase 6.1 — Add a closed rebalancing configuration and UI derivation.** Extend the core configuration with a discriminated union:
    ```ts
    type RebalancingConfig =
      | { readonly mode: 'none' }
      | { readonly mode: 'time'; readonly everyPeriods: number }
      | { readonly mode: 'toleranceBand'; readonly percentagePoints: number }
    ```
    Keep `none` as the default so all current results preserve their behavior. Add raw-string input state, reducer actions, `deriveRunPlan` parsing, and field-specific validation before rendering controls in `PortfolioSettings.tsx`.
    1. **Validate economic units and boundaries explicitly.** Time-based `everyPeriods` must be an integer from $1$ through the configured horizon. Tolerance-band input is in user-facing percentage points, must be finite and satisfy $0< b\le100$, then converts once to a portfolio-weight fraction $b/100$ for core accounting. A zero band is rejected rather than overloaded to mean “rebalance every period”; users choose the explicit time-based mode for that policy.
    2. **Expose only the selected mode’s control.** Render a `none` / `time` / `tolerance band` selector. Time mode shows its period interval; band mode shows its percentage-point threshold and an unambiguous “strictly beyond this drift” label. Do not add costs, taxes, leverage-reset, or calendar-date scheduling controls here: none has an implemented accounting rule until later phases.
    3. **Keep benchmark semantics fixed.** Rebalancing applies only to the portfolio holdings. The Phase 5 benchmark remains one buy-and-hold accumulator receiving the already-determined external `contribution`; it neither rebalances nor receives an artificial cash flow from a portfolio sale.
  - [ ] **Phase 6.2 — Define the pure trade-list contract.** Add `core/portfolio/rebalancing.ts`, exporting immutable `PortfolioTrade` and `RebalanceResult` types plus small pure decision/execution functions. A trade is `{ assetIndex, value }`, where a positive value is a base-currency buy and a negative value is a base-currency sale; entries are in ascending asset-index order and zero-value orders are omitted.
    1. **Calculate targets from post-contribution equity.** Given post-contribution holdings $h_i$, total equity $E=\sum_i h_i$, and target weights $w_i$, the frictionless target is $h_i^*=Ew_i$ and the canonical desired trade is $q_i=h_i^*-h_i$. Every trade is computed from the same pre-trade holdings snapshot, never sequentially after earlier trades have mutated the allocation. This preserves total value and makes the list deterministic.
    2. **Define the trigger rules exactly.** Time mode triggers when `periodIndex % everyPeriods === 0`; period zero is never a rebalance. Tolerance mode computes current weight $h_i/E$ after returns and scheduled contribution and triggers when any $|h_i/E-w_i|>b$. Equality at the threshold does **not** trigger, matching “beyond” rather than “at or beyond.” If $E=0$, no trade is possible or useful; return an explicit no-trade result rather than divide by zero or manufacture a target allocation.
    3. **Apply frictionless trades without hiding future costs.** `applyPortfolioTrades(holdings, trades)` returns new holdings with `holdings[i] + trade.value`. Phase 6 requires every result to remain finite and non-negative within a documented float64 tolerance; a violation is a structured failure, never a clipped holding. The final trade list’s signed values must sum to zero within the same tolerance. Preserve the residual deterministically in the final asset’s trade if needed so Phase 7 can price a value-conserving ledger rather than a rounding leak.
    4. **Return no trade list when not triggered.** `mode: 'none'`, a non-boundary time period, and an in-band tolerance period return the original holdings and an empty immutable trade list. This lets the runner record a normal period uniformly and gives Phase 7 an unambiguous “no orders, no costs, no taxable sale” input.
  - [ ] **Phase 6.3 — Integrate the trade list into the existing period order.** Keep `stepPortfolioPeriod` as the Phase 1 return-plus-contribution primitive. Immediately after it returns, `runSimulation` calls the rebalancing decision/execution function with that period’s holdings, weights, rebalancing config, and one-based period index; the resulting holdings become the next period’s starting holdings.
    1. **Preserve metric and benchmark timing.** `neutralReturn` remains the pre-contribution market return from `stepPortfolioPeriod`; a frictionless reshuffle after contribution has no gain/loss and must not change volatility, Sharpe, drawdown, CAGR, or IRR. The benchmark accumulator consumes the portfolio contribution exactly once, before/independently of rebalancing, so Phase 5’s cash-flow identity remains true under either rebalance method.
    2. **Carry the evidence future accounting needs.** Extend the per-period runner record with the exact `readonly PortfolioTrade[]` for retained paths (period zero has an empty list). The hot loop passes the same list forward immediately; Phase 7 will wrap that point to apply costs and taxes before final holdings are accepted. Do not infer a trade list later by subtracting rounded retained holdings — the original signed values are the financial event.
    3. **Maintain unchanged behavior under `none`.** With default `rebalancing: { mode: 'none' }`, all Phase 1–5 scenarios, values, contribution schedules, representative-path selection, and benchmark terminal results must be identical to their pre-Phase-6 values. Add this as a direct regression test rather than relying on review of the branch.
  - [ ] **Phase 6.4 — Prove rebalancing with deterministic accounting tests.** Add `rebalancing.test.ts` and focused `runSimulation` integration tests before broad UI wiring.
    1. **Hand-calculated time trigger.** Start from a two-asset 50/50 allocation, apply unequal returns and an end-of-period contribution, then verify that a time boundary produces the exact target holdings and signed buy/sell values. Verify the same drift on a non-boundary period produces no trade.
    2. **Tolerance boundaries.** Test one portfolio exactly at its band (no trade), one just beyond it (full rebalance), and multi-asset drift where any one asset’s violation triggers a simultaneous rebalance of every asset to target. Include the zero-equity path and assert it produces an empty list without non-finite weights.
    3. **Frictionless invariants.** For every triggered rebalance, assert signed trades sum to zero, post-trade equity equals pre-trade equity within the declared tolerance, holdings remain non-negative, and post-trade weights equal target weights within tolerance. For every non-triggered result, assert holdings are value-identical and trades are empty. Add a short generated/invariant fixture across several weights and returns as a regression guard, not as a substitute for the hand arithmetic.
    4. **Runner/benchmark integration.** Run a scripted value-averaging path with and without rebalancing: portfolio holdings and terminal wealth may differ only through subsequent asset-return exposure, but the recorded contribution sequence and benchmark terminal value for the same scenario sequence must be identical. Confirm retained-path trade lists match the pure module’s exact output.
  - [ ] **Phase 6.5 — Present the controls and close the phase.** Wire the Phase 6 controls into `PortfolioSettings.tsx`, render the completed run’s rebalancing mode in the prior-run label, and add a retained-path table column summarizing each period’s orders (for example, `AAA sell $120; BBB buy $120`). A zero-order period is labelled “none”; do not claim realized gains, costs, tax, or turnover until Phase 7 implements those concepts.
    1. **Accessibility and interaction checks.** Add labels, mode-specific error placement, and an `aria-live` validation summary through the existing field-error system. Changing mode or thresholds during a run follows the current cancel-on-input-edit rule; results remain labelled with their completed configuration.
    2. **Completion gate.** Focused pure/reducer/runner tests; full Vitest suite; `npm run lint`; `npm run format`; `npm run test:e2e`; and `npm run build`. Browser smoke coverage must exercise no-rebalance, a time-based run, and a tolerance-band run, including the retained-path trade display. Document the signed-trade convention, trigger boundary rule, frictionless conservation result, benchmark invariance, and validation results in `LOG.MD` before checking off Phase 6.
- [x] **Phase 7 — Transaction costs, then taxes.** Second and third items of Implementation Order step 6. This phase turns Phase 6's frictionless signed trade intent into a cash-funded execution ledger, then applies tax to its actual sale proceeds. The order is non-negotiable: sell costs reduce proceeds before realized gain is measured, and buy costs become part of average cost basis. It preserves the existing benchmark contract: the benchmark receives the portfolio's gross external contribution and remains frictionless, while only the portfolio bears fees and taxes. The Phase 1 return-plus-contribution primitive and Phase 6's canonical `PortfolioTrade[]` stay intact; a cost/tax execution wrapper consumes their outputs. There is no leverage or margin call in this phase, but the sale-execution contract must remain reusable by Phase 8.
  - [x] **Phase 7.1 — Add closed cost and tax configuration with honest UI derivation.** Extend `SimulationConfig` with immutable `TransactionCostConfig` (`fixedPerOrder`, `proportionalRate`) and `TaxConfig` (`capitalGainsRate`, `initialCostBasis: number | null`), keeping zero costs, zero tax, and `null` basis override as defaults so completed Phase 1–6 runs remain byte-for-byte unchanged.
    1. **Define units and validation at the core boundary.** Fixed cost is non-negative base-currency money per executed nonzero order; proportional cost is a non-negative fraction of absolute order value; capital-gains tax is a fraction in $[0,1]$; an initial basis override is finite and non-negative. Reject invalid inputs with structured `config.transactionCosts.*` and `config.tax.*` errors, never clamp a rate or replace a malformed basis with zero.
    2. **Add raw-string input state only after the core types validate it.** Extend `SimulatorInputs`, reducer actions, `deriveRunPlan`, and `PortfolioSettings.tsx` with fixed cost, proportional cost, capital-gains tax, and optional initial aggregate basis fields. The UI displays percentage rates in percentage points and converts them once to fractions in the run plan; leaving the initial-basis field blank means the initial investment is the basis. Field-specific errors and the existing `aria-live` summary remain the only validation surface.
    3. **Keep external-cash-flow meaning fixed.** Initial investment is allocated at period zero without a Phase 7 order cost, because this phase's cost scope is scheduled contributions and Phase 6 rebalancing orders. Each later DCA/value-averaging contribution remains the investor's gross external cash flow for the benchmark, loss probability, and IRR even when part of it is consumed by purchase costs.
  - [x] **Phase 7.2 — Build a pure transaction-cost execution ledger.** Add `core/portfolio/transactionCosts.ts` with immutable order/result types and colocated tests. It accepts only explicit signed trade intent, contribution cash, holdings, and the validated cost configuration; it does not choose rebalancing thresholds, calculate taxes, mutate a caller's holdings, or reach into the runner.
    1. **Price every executed order deterministically.** For an order of absolute gross value $|q|$, charge $f + p|q|$, where $f$ is the fixed fee and $p$ is the proportional rate. Omit a zero-value order entirely, so it incurs neither fee nor a fabricated trade. Return itemized buy and sell fee totals plus per-order fees; Phase 7 accounting must record the actual economic event rather than reconstruct a total from rounded holdings later.
    2. **Fund scheduled contributions before allocating their purchases.** For a gross contribution $C$ and the positive-weight asset set, solve the net purchasable gross value $B$ from $C=B+n_bf+pB$, then allocate $B$ at target weights. Thus every dollar has one destination: purchase value or an explicit order fee. If the realised contribution cannot fund all required nonzero orders and their fixed costs, return a structured path failure rather than creating negative holdings, an implicit cash balance, or a silently smaller external contribution.
    3. **Fund rebalancing buys from post-cost, post-tax sale proceeds.** Preserve Phase 6's canonical sell intent, execute those sales first, and let the later tax ledger report the tax due on them. Scale only the intended buy leg so its gross purchases plus buy fees exactly fit remaining sale proceeds after sell costs and tax. This deliberately leaves the portfolio near, rather than falsely exactly at, target weights when friction makes an exact self-financing rebalance impossible; no borrowing or unmodelled cash account is introduced.
    4. **Define explicit feasibility and conservation outcomes.** An insufficient-sale-proceeds execution, non-finite fee, non-finite post-trade holding, or holding below the documented float64 tolerance is a stable path failure. Successful execution returns new non-negative holdings, executed orders, gross contribution, total fees, and available sale proceeds. Time complexity is $\mathcal{O}(D)$ and space complexity is $\mathcal{O}(D)$ for $D\le6$; document the ledger's array layout and its distinction between intent and executed orders in code.
  - [x] **Phase 7.3 — Build pure average-basis and loss-carryforward accounting.** Add `core/portfolio/taxes.ts` with a per-path state containing one aggregate dollar basis per portfolio asset and one non-negative path-level loss carryforward. The initial basis defaults to `initialInvestment`, or the user override, allocated at target weights; it is not inferred from current market values.
    1. **Apply buys and sales in their economic order.** A buy increases that asset's basis by its gross purchase value plus its own buy cost. For a sale from holding value $H_i$, dispose the proportional basis $\mathrm{basis}_iS_i/H_i$; realized gain is net sale proceeds (gross sale less its sell cost) minus disposed basis. Update remaining basis by subtracting only the disposed basis, so market moves never themselves alter tax basis.
    2. **Use one loss carryforward across the whole path.** A realised loss adds $-g$ to carryforward. A realised gain first consumes available carryforward; tax is `capitalGainsRate * max(0, gain - carryforwardUsed)`. Apply tax immediately to the portfolio's available sale proceeds, never create a refund for excess carryforward, and retain unused loss carryforward for later sales in the same path.
    3. **Expose an auditable tax result.** Each pure operation returns updated bases/carryforward plus disposed basis, net gain/loss, carryforward used/created, and tax paid. Validate sale fractions and basis values explicitly; a sale exceeding the current holding or non-finite state returns a structured failure, never a negative-basis repair. Phase 8 will reuse this same sale path for forced deleveraging.
  - [x] **Phase 7.4 — Integrate costed execution, taxes, and final liquidation into the runner.** Extend `runSimulation` after `stepPortfolioPeriod` and Phase 6 trade-intent generation, retaining the existing scenario sequence, selection projection, and benchmark step unchanged.
    1. **Apply the per-period ledger in the README's order.** After returns and after computing the gross scheduled contribution, construct contribution buys; execute rebalancing sales, price them, apply the tax state, then fund and execute rebalancing buys from the remaining proceeds. The next period starts from the resulting holdings. The benchmark consumes the original `result.contribution` once before this portfolio-only ledger, preserving the Phase 5 cash-flow identity for DCA and path-specific value averaging.
    2. **Use post-ledger equity for wealth and contribution-neutral metrics.** Store period equity after transaction costs and immediate taxes. Feed metrics the accounting-neutral return $(E_t-C_t)/E_{t-1}-1$, where $C_t$ is the unchanged gross external contribution, so trading friction is performance drag but saving behavior is not investment return. With zero cost/tax settings this reduces exactly to the existing `neutralReturn`; add a direct regression test for that identity.
    3. **Liquidate exactly once at the terminal boundary.** After the final simulated period, create sell orders for every nonzero holding, price sell costs, dispose proportional basis, offset gains with remaining loss carryforward, deduct immediate final tax, and set `terminalWealth` to the residual cash. The benchmark remains unliquidated and frictionless by design. Feed the liquidation drag into the final period's contribution-neutral metric before finishing its accumulator, so terminal wealth, CAGR/IRR, volatility, Sharpe, and drawdown use one consistent after-cost, after-tax portfolio definition.
    4. **Retain only bounded audit detail.** Keep aggregate all-path terminal outcomes and metrics as today; for retained paths add per-period executed orders, total transaction costs, realised gains/losses, tax paid, remaining basis, and loss carryforward. Do not add an all-path $N\times T$ tax matrix. Update `SimulationResult`, result metadata algorithm versions, Worker transfer handling for new typed arrays only, and stable failure codes without changing the benchmark terminal-buffer contract.
  - [x] **Phase 7.5 — Present cost and tax evidence without overstating it.** Extend the completed-run label and `ResultsPanel` to show the actual cost/tax settings that produced the result, terminal wealth explicitly labelled after liquidation, and distribution summaries for cumulative transaction costs, realised gains/losses, tax paid, and remaining loss carryforward where defined.
    1. **Make retained-path accounting inspectable.** Add table columns or a compact period detail view for executed buys/sells, order costs, realised gain/loss, tax, basis, and carryforward. Display the executed cost-aware orders, not Phase 6's frictionless intent alone; zero-event periods read `none` rather than implying a zero tax calculation was skipped.
    2. **Preserve state and accessibility rules.** New controls have visible labels, percentage units, field-addressable errors, keyboard access, and concise explanatory text. Editing a cost, tax rate, or basis while a run is active follows the existing cancel-on-input-edit rule; completed results continue to render their own immutable configuration and never adopt the next form's settings.
  - [x] **Phase 7.6 — Prove the ledger before closing the phase.** Add focused pure tests, runner integration tests, UI tests, browser smoke coverage, and an independent Python trace before broadening into leverage.
    1. **Hand-calculated transaction-cost tests.** Cover one contribution whose gross cash is split into purchases and fees, a fixed-plus-proportional rebalance funded by sales, zero-value orders, insufficient contribution/sale proceeds, and zero-cost compatibility. Assert no successful ledger creates or destroys value except through the explicit fee/tax debits.
    2. **Hand-calculated tax tests.** Cover initial-basis default and override, average-basis purchases, partial sales, sell costs reducing proceeds before gain calculation, losses offsetting later gains across different assets, no tax refund, and final liquidation. Assert basis conservation: before a sale, basis equals disposed basis plus remaining basis within float64 tolerance.
    3. **Runner/benchmark/metric invariants.** Use a scripted multi-asset engine with DCA and value averaging to assert the benchmark still receives every gross portfolio contribution, no-benchmark zero-cost/zero-tax runs remain byte-for-byte compatible with Phase 6, terminal wealth is after final liquidation, and retained ledger details agree with the pure modules. Verify `ruinProbability` does not reinterpret a fee/tax path failure as leverage insolvency before Phase 8 defines that distinction.
    4. **Independent cross-check and completion gate.** Extend `validation/phase_1_6_independent_check.py`, or add a narrowly named Phase 7 companion, with a procedural multi-period trace covering contribution costs, rebalance sales, basis updates, carryforward, and final liquidation. Assert TypeScript period-by-period values against that independently derived trace. Run focused tests, the full Vitest suite, `npm run lint`, `npm run format`, `npm run test:e2e`, and `npm run build`; manually exercise Bootstrap and Parametric runs with DCA, value averaging, rebalancing, and a benchmark. Record the funding policy, tax convention, memory decision, and validation results in `LOG.MD` before checking off Phase 7.
- [ ] **Phase 8 — Leverage and margin calls.** Add `core/portfolio/leverage.ts`: long-only target gross exposure $1\le L\le\min(4,1/m)$ with editable maintenance margin $m$, debt defined as gross asset value minus equity, borrowing charged at the sampled base-currency short rate plus a user-entered spread, weekly-locked frequency, time-based and tolerance-band leverage resets, and the per-period margin check — proportional forced selling (through Phase 7's tax and cost machinery) until target leverage is restored, insolvency marking the path with zero terminal wealth when it cannot be. Phase 4.2's ruin probability becomes live here, as does the spec's weekly leverage-ratio chart and the $L=1/m$ no-buffer warning. Invariant tests: debt repayment, exact maintenance-margin boundaries, insolvency.
- [ ] **Phase 9 — CSV export.** Last item of step 6. Export settings, seed, dataset version and checksum, all algorithm versions, summary metrics, and terminal outcomes for **all** completed paths; full period-by-period detail only for the ≤50 retained paths, keeping browser memory bounded per the Visualization spec. Pure serialization functions in `core/`, tested headlessly; the download wiring is a thin component.
- [ ] **Phase 10 — Second frequency, then second base currency.** Monthly USD artifact first (the unleveraged advanced option: 60-month minimum, same pipeline and validation gate as `usd-weekly-v1`), unlocking Phase 4.5's disabled frequency control. Then EUR — only after its complete FX-route (`fx_routes.csv`, currently intentionally empty), HICP inflation, EUR short-rate, and provenance pipeline passes the exact validation the browser applies, per Implementation Order step 7. No `src/` engine changes: both are new artifacts flowing through the existing loader contract.
- [ ] **Phase 11 — Markov-chain engine.** Step 8, last by design: only after both live engines and the full accounting path are validated. Same `PeriodScenario` boundary and aligned data, emitting complete regime-compatible rows with a regime ID in scenario metadata; deterministic transition-matrix and accounting tests; a third `EngineSelection` variant (the exhaustive `switch` in `executeRun` makes forgetting it a compile error); and the disabled "Markov Chain (future)" button finally becomes live.